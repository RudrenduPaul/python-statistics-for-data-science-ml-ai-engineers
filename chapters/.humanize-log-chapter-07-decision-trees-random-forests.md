# Humanize log: chapter-07-decision-trees-random-forests.md

Mode: BOOK (auto-inferred from `books-all/` path)
Scope: this pass covers ONLY the two sections added in this session (the rest of the chapter,
including the pre-existing "Evaluating a classifier: precision, recall, and the ROC curve"
section, was not touched and is out of scope here):
- "Permutation importance, and where Gini and entropy importance mislead" (`#sec-importance-comparison`)
- "Decision boundaries: a tree, a forest, and logistic regression compared" (`#sec-decision-boundaries`)
Word count of scoped content: ~1,255 words (Small bucket per the skill's size gate).
Run type: condensed pass (author self-review against the full pattern list, plus one independent
fresh-context critique subagent standing in for Critique-A + Critique-B), not the full 8-12
subagent 4-pass pipeline. Proportionate to the scoped word count, matching the precedent set in
chapter 1 and chapter 3's humanize logs for this same book.

## What was checked

- Em dashes: zero found (also blocked at write time by the project's hook, which rejects every
  em dash and every banned precision word before a file can save).
- The skill's AI-vocabulary list (Pattern 7): zero hits.
- Staccato stacks, exhaustive parallelism lists, compound workflow decompositions, superficial
  -ing analyses, enumerated framework scaffolding, synonym cycling, paragraph-terminal summaries,
  significance inflation, promotional language, conjunctive openers, filler phrases, excessive
  hedging, generic positive conclusions, authority tropes: none found.
- B4 callout density: 2 callouts across ~1,255 words, well under the 1-per-500-word threshold.
- B6 list overuse: no bulleted or numbered lists in the new content.
- B7 running-example continuity: "rollback forest," "canary error rate," "dependency count," and
  "deployment batch ID" are named consistently and match the chapter's established terminology.
- Pseudo-precise statistics: every number in the new content traces directly to the shipped
  `_generated/*.html` figure output (checked against the rendered figure itself, not a
  standalone scratch run), so none are invented.

## Fixes applied

1. Heading "Comparing decision boundaries: a tree, a forest, and logistic regression" (gerund
   opener, Pattern B1, and inconsistent with this chapter's own noun-phrase-plus-colon heading
   convention) -> "Decision boundaries: a tree, a forest, and logistic regression compared."
2. "A feature whose shuffled version costs the model nothing was not doing useful work inside
   the model to begin with" (Pattern 9, definitional negation) -> "was carrying no predictive
   weight inside the model."
3. "This matters for a concrete reason beyond aesthetics" (Pattern 53, vague "This" subject) ->
   "The gap between these boundaries matters for a concrete reason beyond aesthetics."
4. "None of the three boundaries here is a certified best fit. This dataset is simulated, so any
   of the three could plausibly sit closest to the rule that generated it." (bare negation
   opener) -> "Each boundary here is one plausible read of this simulated dataset: any of the
   three could sit closest to the rule that generated it."

## Not applied

- Contraction insertion (Pattern 30): skipped. This chapter follows the book-writing-style-islr
  voice, a formal textbook register that does not contract, consistent with how chapters 1 and 3
  handled this same pattern.
- Full 4-pass worker architecture with staggered batches and URL verification: skipped in favor
  of a direct self-review plus one independent critique subagent, proportionate to a ~1,255-word
  scoped addition rather than a full chapter draft.
- The critique subagent flagged several pre-existing trailing "X, not Y" constructions elsewhere
  in the new content (for example "not in picking a winner from the picture alone") as fitting
  a deliberate voice device established throughout the rest of this chapter's unmodified prose,
  so these were left as-is rather than stripped, to keep the book's own authorial voice intact.

## Verdict

Clean after fixes. No outstanding AI-pattern issues in the scoped content.
