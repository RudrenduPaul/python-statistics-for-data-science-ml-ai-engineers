# Humanize log: chapter-04-regression-modeling.md

Mode: BOOK (auto-inferred from `books-all/` path)
Run type: condensed pass (zero-tolerance + voice self-review), consistent with chapters 1-3.

## What was checked

- Em dashes and the project's banned precision-word list: caught and fixed during drafting via
  a scratch-file grep pass mirroring the write-time hook's own whole-word matching, before the
  final Write call. Final saved version is clean.
- Negative-parallelism density: reviewed. Remaining "not X" constructions carry substantive
  content (for instance, "R-squared always rises as predictors are added, even useless ones"
  paired with "adjusted R-squared does not" states a contrast the chapter's own figure
  demonstrates, not a rhetorical tic).
- Gerund headings, paragraph-terminal summaries, staccato stacks, demonstrative labeling: none
  found on review.
- Running example continuity: the payload-size-to-latency OLS model, confidence/prediction
  intervals, timeout-probability logistic regression, and the regularization example all
  extend the checkout-API narrative and callback directly to Chapter 1's covariance/correlation
  section and Chapter 2's p-value/ASA discussion.
- Citation accuracy: all three regularization/AIC citations were checked via WebSearch against
  the published journal record; see the chapter's plagiarism report.
- Mathematical accuracy: the raw source's Cp statistic formula was checked against the standard
  definition and found to be incorrect; this chapter does not repeat the error.

## Verdict

Clean. No outstanding AI-pattern issues.

---

## Addendum: "A Bayesian perspective" section added (2026-08-31)

Added a closing section (roughly 640 words) covering Bayesian linear regression, the
Ridge-as-Gaussian-prior and Lasso-as-Laplace-prior equivalence, and Bayesian logistic
regression, plus one new figure (`fig_bayesian_ridge`) showing a coefficient's posterior
narrowing as the prior variance shrinks.

Checked and fixed on review: two negative-parallelism constructions ("is not a different
technique... it is", "is not an arbitrary tuning knob... it is") rewritten to state the
positive claim directly, matching the standard applied to the rest of this chapter and to
chapters 1-3. No em dashes, no banned precision words, no staccato stacks, no gerund headings
in the new section. The closing paragraph's forward reference was updated from "Part 2" to
"Part 3" to match the book's current structure (Part 2 is now Regression and Predictive
Modeling; Bayesian A/B testing moved to Part 3).

## Addendum: visual QA and storytelling-gap pass (2026-09-01)

Re-verified all 6 existing figures against the code that generates them, and visually against a
live browser render of each `_generated/*.html` file. Numerically re-derived the confidence-
interval/prediction-interval math in `fig_ci_vs_pi` independently in a scratch script; confirmed
CI never exceeds PI bounds and the pinch point sits at x-bar, so the apparent visual "notch"
where the two translucent bands overlap is a rendering artifact of alpha blending, not a data
bug. Removed three orphaned figure files in `_generated/` (`ols-confidence-prediction`,
`r2-adjusted`, `regularization-path`) left over from an earlier draft and never referenced by
the current `.md`.

Found one storytelling gap worth closing: AIC and BIC were introduced with formulas and a
citation but no supporting figure, unlike every other metric in this chapter. Added
`fig_aic_bic`, tracking AIC/BIC for three candidate models (payload-only, plus region, plus a
noise predictor) as sample size grows, which makes the "BIC penalizes complexity more heavily as
sample size grows" claim concrete instead of asserted. Verified the new figure's underlying
regression numbers by hand in a scratch script before embedding it. New prose: one
negative-parallelism construction found and rewritten on self-review; no em dashes, no banned
precision words.

Also confirmed a book-wide, cosmetic-only rendering quirk worth flagging to the coordinator: a
Plotly slider's displayed data on first load matches `"active": 0` correctly, but simple clicks
on the slider track advance one frame per click regardless of where the track is clicked, so a
single click can land on an unexpected frame. Dragging the handle, or clicking repeatedly,
reaches every frame and the label matches the displayed data once clicked. This affects the
interaction pattern shared by all figures in the book, not the data itself, and is not something
this pass attempted to fix given the uncertainty around Plotly.js's internal slider-click
handling.

## Addendum: what/why/how and rigor pass (2026-09-01)

Two edits, both matching the chapter's existing formal, ISLR-style register.

- **AIC/BIC citation fix**: the paragraph comparing AIC's and BIC's sample-size behavior cited
  only Akaike (1974), which is the AIC paper, for a claim about BIC. Added Schwarz (1978), the
  paper BIC comes from, verified live via WebSearch and added to a new fragment file,
  `chapters/.refs-chapter04-whywhyhow.bib`, rather than editing `references.bib`.
- **Logistic regression narrative gap**: the section defined the log-odds coefficient
  correctly but never stated what goes wrong if a reader mistakes it for a direct probability
  shift. Added two sentences tying the misreading to a concrete consequence: the same one-unit
  change in the predictor moves probability by a much larger or smaller amount depending on
  where on the curve the model currently sits.

New prose checked for em dashes and the project's banned-vocabulary list: none found. No
structural AI patterns introduced.

## Addendum: 2026-09-01, plain-language layer pass

Added six short (3-4 line) plain-language intuition paragraphs, one before each major concept's
formula: least-squares line fitting, confidence vs. prediction intervals, the logistic curve,
adjusted R-squared/AIC/BIC, regularization, and Bayesian priors/posteriors. Each uses a concrete
everyday analogy rather than a shorter restatement of the technical framing. Skipped statistical
vs. practical significance (accessible without heavy notation) and the credible-interval closing
paragraph (worded simply enough). Checked for em dashes and banned vocabulary: none found.

## Addendum: 2026-09-01, sentence-level clarity pass

Fourth pass: a close, sentence-by-sentence clarity edit of the prose (not a content pass; every
formula, citation, figure, and fact is unchanged). Roughly 20 sentences touched, concentrated in
the sections that had accumulated the most edits across prior passes (OLS assumptions, the
confidence/prediction-interval closing paragraph, logistic regression's coefficient warning,
AIC/BIC, the Ridge/Lasso mechanism comparison, and the Bayesian perspective section). Kinds of
edits made:

- **Split overlong sentences** (35+ words, often stacked with a semicolon or two "and"/"which"
  clauses) into two shorter sentences, one claim per sentence. Example: the OLS-assumptions
  sentence chaining "meaning both homoscedasticity and normality are..." with "when they fail
  badly enough, a generalized linear model..." via a semicolon became two full sentences.
- **Removed filler openers.** "Note that this distinction matters operationally" became "This
  distinction matters operationally," cutting a redundant announcement before the point itself.
- **Replaced dangling "which" appositives with a direct second sentence** so the reader is not
  left backtracking to find the referent, for instance the logistic-curve dimmer-switch sentence
  and the AIC/BIC n=400 sentence.
- **Tightened one redundant phrase.** "A topic beyond the scope of this chapter but worth
  knowing exists" became "a topic beyond this chapter's scope but worth knowing about."

Checked for em dashes and the project's banned-vocabulary list: none found. No structural AI
patterns introduced. Re-ran `/plagiarism-check`; see the addendum in the chapter's
`.plagiarism-report.md` dated the same day. Verdict: clean, no outstanding issues.

## Addendum: 2026-09-01, second clarity pass

A second, higher-bar clarity pass following the fourth-pass edit above. Most of the chapter read
cleanly going in; this pass touched five spots:

- **Ambiguous pronoun.** In the logistic-regression opener, "It applies the logistic function"
  followed a sentence about "that shape," making "It" briefly ambiguous between the dimmer-switch
  analogy and logistic regression itself. Changed to "Logistic regression applies the logistic
  function."
- **Inconsistent backward-reference verb.** "Note that Chapter 2's Type I and Type II error
  framework applies directly here" was the only backward reference in the chapter (and one of
  only two across chapters 4-6) not using "Recall," the book's established signal word. Changed
  to "Recall Chapter 2's Type I and Type II error framework."
- **Terminology inconsistency.** The regularization section called the time-of-day variable a
  "noise feature" twice, while the AIC/BIC section (and this chapter's dominant term throughout,
  used 20+ times) calls the same kind of variable a "noise predictor." Changed both instances to
  "noise predictor" to match, and flagged the same inconsistency in Chapter 5's opening paragraph
  for a matching fix there.
- **Double-comma appositive requiring a re-read.** "That trade, more computation for a fuller
  picture of uncertainty, is worth returning to" became "That trade of more computation for a
  fuller picture of uncertainty is worth returning to," removing the parse-twice construction
  without changing the claim.

No formula, citation, figure reference, fact, or number was changed. Checked for em dashes and
the project's banned-vocabulary list: none found. Re-ran `/plagiarism-check`; see the matching
addendum in `.plagiarism-report.md`. Verdict: clean, no outstanding issues.

## Addendum: 2026-09-01, seventh pass, teaching-clarity edit

A full word-by-word re-read for teaching clarity, on top of the six prior passes. Five gaps
found and fixed, all additions grounding an existing formula or claim rather than new content:

1. The logistic function formula had a plain-language lead-in before it (the dimmer-switch
   analogy) but no restatement after it, unlike most other formulas in the chapter. Added one
   explaining why the formula's output stays between 0 and 1 as $x$ grows or shrinks.
2. The adjusted R-squared formula was given with its symbols named but no explanation of the
   mechanism by which it counteracts R-squared's automatic rise. Added a sentence unpacking why
   the $(n-1)/(n-p-1)$ term grows as predictors are added relative to sample size.
3. The AIC/BIC formulas named $\hat{L}$ and $k$ but never unpacked what $-2\ln(\hat{L})$
   measures or why it moves the direction it does as fit improves. Added a restatement.
4. The Lasso formula introduced $\lambda$ with no definition at its first appearance; the term
   "regularization strength" for $\lambda$ did not appear until several sentences later, after
   the Ridge and Elastic Net formulas. Added a definition at first use, including the $\lambda =
   0$ edge case (plain OLS).
5. The Bayesian-perspective section used "posterior mean" for the flat-prior/OLS case and
   "posterior mode" for the Ridge/Lasso case without explaining the relationship between the two,
   an inconsistency a careful reader could read as sloppy or as a hidden distinction. Added two
   sentences: a Gaussian posterior's mode and mean coincide (so the terms are interchangeable in
   the Ridge case), while a Laplace-prior posterior's mode and mean can differ, and it is
   specifically the mode that matches the Lasso estimate.

Anti-sycophancy self-check: reviewed the first draft of edit 1 and found an accuracy problem
before saving. The initial phrasing stated that $p(x)$ approaches 1 as $x$ grows, without
qualifying that this direction only holds when $\beta_1$ is positive; for a negative $\beta_1$
the direction reverses. This would have been wrong for any logistic model with a negative
coefficient, so it was rewritten to state the $\beta_1$-positive case explicitly (matching the
chapter's own load-predicts-timeout example) and to note that a negative $\beta_1$ reverses the
direction while the 0-to-1 bound holds regardless of sign. No other edits in this pass were found
to drop a caveat or overstate a claim on review. Zero em dashes, zero banned precision words.
Re-ran `/plagiarism-check` on the five new passages; see this chapter's plagiarism report
addendum for the same date. No other sections touched.

## Addendum: 2026-09-01, figure-first restructure, caption, and callout pass

Fifth structural pass, covering the five parts of a book-wide editing task run across chapters
4 through 6.

1. **Figure placement.** All 7 figure divs moved so each appears immediately after its section
   heading (or after the shortest possible formula/definition setup, as in the OLS section,
   where the div now sits before the "picture drawing one line" analogy). Every sentence that
   used to introduce a figure by name ahead of the image ("@fig-ols shows this fit...") now
   follows the image instead, reworded from future-tense setup to a description of what the
   reader had just seen.
2. **Captions.** All 7 captions rewritten to state the chart's takeaway rather than only name
   its axes. The AIC/BIC and logistic-timeout captions were the thinnest in the original (mostly
   axis description); both now open with the finding.
3. **Visual inspection.** Opened all 7 PNGs with the Read tool and checked each against its
   surrounding prose for wrong numbers, wrong direction, or wrong axis labels. No mismatch found
   in this chapter: the OLS/noise relationship, the CI-narrows/PI-stays-wide pattern, the
   logistic S-curve direction, the R-squared/adjusted-R-squared divergence, the AIC/BIC bar
   heights, the Lasso-zeroes/Ridge-shrinks contrast, and the Bayesian-ridge posterior narrowing
   all match what the prose claims. (The logistic-timeout PNG shows a non-monotonic loop in the
   fitted curve at high load, an artifact of how this book's interactive Plotly figures render to
   a static PNG, not a data or prose problem; flagged for the coordinator, not fixed here per the
   "do not regenerate figures" constraint.)
4. **Paragraph length.** Roughly a dozen paragraphs over 4 lines split at natural sentence
   breaks, concentrated in the OLS-assumptions list, the confidence/prediction-interval
   definitions, the logistic $p(x)$ derivation, the AIC/BIC citation paragraph, and the
   model-interpretation section.
5. **Callouts.** Four added: a `.callout-tip` on residual plots as a practical assumption check,
   a `.callout-note` on choosing between a confidence and a prediction interval, a
   `.callout-warning` on misreading a logistic coefficient as a probability shift, and a
   `.callout-note` on Lasso's arbitrary pick within a correlated predictor group.

No formula, citation, fact, or number was changed. Zero em dashes, zero banned precision words
(verified by grep across the full file after editing). Re-ran `/plagiarism-check`; see the
matching addendum in `.plagiarism-report.md` dated the same day. Verdict: clean, no outstanding
issues.

## Addendum: 2026-09-01, ninth pass, figure-placement reversal and follow-up split/callout pass

This pass revisits point 1 of the prior addendum's figure-first restructure under updated
editorial guidance: a figure div should sit at or before the paragraph that discusses it in
depth, but never earlier than the point where the reader has the background needed to read it.
The prior pass's rule ("each figure div appears immediately after its section heading") put five
of the seven figures ahead of any defining prose: the OLS scatter appeared before the words
"ordinary least squares" were defined, the CI/PI band chart before the confidence/prediction
distinction, the logistic S-curve before the dimmer-switch analogy and the logistic function
itself, the R-squared/adjusted-R-squared line chart before either metric was named, and the
Lasso/Ridge coefficient-path chart before regularization was introduced. A reader hitting the
image first would see axis and legend language (P(timeout), noise predictor, regularization
strength) undefined.

Moved those five figure divs down to sit immediately before the paragraph carrying their
`@fig-id` reference, after the minimum setup (the plain-language analogy and, where present, the
formula) needed to read the chart. `fig-aic-bic` and `fig-bayesian-ridge` were placed this way in
the prior pass and needed no change.

Splitting each of those five sections' lead-in text at the new figure boundary produced two of
this pass's six paragraph splits (OLS, confidence/prediction intervals) for free; the other four
(the AIC/BIC usage-guidance paragraph, the regularization intro, and two sentences in the
Bayesian-perspective section) were split independently after this pass found six paragraphs
running 5-6 source lines, past the chapter's 4-line convention. Also rewrote five captions (OLS,
CI/PI, logistic, R-squared, regularization) to name the plotted axes and legend colors rather
than only stating the takeaway, since the prior pass's captions led with the finding but did not
always say what was on each axis; the AIC/BIC and Bayesian-ridge captions were left as written.

Three callouts added, one each in the sections that had none of their own going in (Measuring
fit, Model interpretation, A Bayesian perspective): a warning that AIC/BIC scores are only
comparable across models fit on the same dataset and outcome variable, a reminder that a large
sample makes almost any coefficient statistically significant so effect size is what to check,
and a note that a prior is a choice that can bias a result if picked carelessly. None restates an
existing callout in this chapter.

Anti-sycophancy self-check: compared every rewritten caption against its PNG (axis labels, curve
colors, legend) before saving; found and fixed one awkward phrase in the OLS caption ("stays
close to true") that read as ambiguous rather than wrong, tightened to "stays close to correct."
No paragraph split changed a claim, dropped a qualifier, or reordered a causal chain; each split
falls at an existing sentence boundary. No formula, citation, fact, or number was changed. Zero
em dashes, zero banned precision words. No `/plagiarism-check` web sweep needed: the new callouts
state standard statistical properties implied by this chapter's own definitions, not new external
claims requiring a source.

## Addendum: 2026-09-05, multiple regression, categorical encoding, interactions, VIF, and
classification metrics gap-fill pass

Tenth pass, following this project's condensed-pass convention (self-review against the full
pattern list rather than a spawned multi-agent run, matching how every prior pass on this chapter
was conducted). Scope: the six new sections added this pass (Multiple regression, Categorical
predictors and dummy encoding, Binning high-cardinality categorical predictors by residual,
Interaction terms, Multicollinearity and the variance inflation factor, Classification metrics)
and no other section.

Zero-tolerance check (Pass 1 equivalent):

- Em dashes: none, verified by grep across the full file.
- The project's standard AI-vocabulary banned list: none found.
- Negative parallelism: two trailing "X, not Y" constructions found and rewritten to state the
  positive claim directly. "Belongs to load, not payload" became "is load's own effect, showing
  up under payload's name because the two move together in this sample." "Groups endpoints by
  shared behavior instead of by name" became "groups endpoints that behave alike, whatever each
  route happens to be called."
- One candidate kept on review: "delta is not an adjustment to the intercept the way gamma is;
  it is an adjustment to the slope itself." Kept because it states a methodological contrast
  (what an interaction coefficient adjusts versus what a main-effect dummy coefficient adjusts),
  the exception the audit file's cross-chapter voice conventions carve out for substantive
  contrasts. The OLS section's "does not bias... it inflates" sentence and the VIF section's
  "does not mean... it means" sentence use the identical construction and were kept under the
  same rule in earlier passes.

Structural check (Pass 2 equivalent):

- No exhaustive parallelism lists, compound workflow decompositions with five or more verbs,
  superficial -ing analyses, or enumerated framework scaffolding.
- Term consistency held across all six sections: "concurrent load," "payload size," "region,"
  and "endpoint" stay fixed rather than drifting to synonyms.
- No paragraph-terminal summary sentences and no vague pronoun subjects lacking a clear noun.
- New headings ("Multiple regression: more than one predictor at a time," "Categorical
  predictors and dummy encoding," "Binning high-cardinality categorical predictors by residual,"
  "Interaction terms," "Multicollinearity and the variance inflation factor," "Classification
  metrics: accuracy, precision, and recall") match the topic-phrase or topic-colon-elaboration
  heading style used throughout this chapter (for example "Ordinary least squares regression"
  and "Measuring fit: R-squared, adjusted R-squared, AIC, and BIC"), not a gerund style this
  book has never used.

Language check (Pass 3 equivalent):

- No significance-inflation words (crucial, vital, and similar), promotional language,
  sentence- or paragraph-opening conjunctions ("Moreover," "Furthermore," "Additionally"),
  filler phrases, or hedge-stacking were found.

Voice check (Pass 4 equivalent, book-mode adapted):

- Contractions: zero, matching the audit file's documented rule for this chapter ("no
  contractions, formal ISLR-style register"), a book-mode override of the humanize skill's
  default contraction-insertion behavior.
- Scenario openers: the new Interaction terms section opens with "Suppose eu-west runs on a
  leaner fleet than us-east...," continuing the "Suppose..." convention the CI/PI, logistic
  regression, and regularization sections set earlier in this chapter.
- Figure placement: every new section shows its figure before the formal notation, matching the
  figure-placement convention set in the ninth-pass addendum above.

Formatting sweep (P19-P26 equivalent):

- Bold: none added; new terms use italics on first mention (Multiple regression, dummy variable,
  interaction term, variance inflation factor, confusion matrix), matching this chapter's
  existing convention.
- No inline-header bullet lists, emoji, or decorative hyphenated filler compounds. Terms like
  high-cardinality, checkout-API, and capacity-planning are precise, established technical
  terms, not filler.
- Curly quotes: zero matches on a Unicode-range grep (U+2018, U+2019, U+201C, U+201D).
- No small comparison tables introduced. All six new headings are level-2, matching every
  existing section.

Anti-sycophancy self-check: every worked number cited in the new sections (payload coefficients
9.95/9.26/9.20 ms/KB, the 0.78 payload-load correlation, the 16.3 ms raw versus 8.3 ms adjusted
us-west gap, the interaction SSE pair 29,039/29,038 and 47,537/29,038, the VIF sequence from 1.12
to above 1,300, the 0.662-to-0.833 R-squared jump in the endpoint-binning example, and the five
confusion-matrix threshold rows) was checked against a fresh, independent execution of
chapter-04-regression-modeling-plots.py's functions in this session, not against memory of the
first draft; every number matched. Zero em dashes, zero banned precision words, verified by grep
across the full file after editing. Re-ran plagiarism-check on the new sections; see the matching
addendum in .plagiarism-report.md dated the same day. Verdict: clean, no outstanding issues.
