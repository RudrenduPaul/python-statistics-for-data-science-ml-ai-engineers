# Humanize log: chapter-02-hypothesis-testing.md

Mode: BOOK (auto-inferred from `books-all/` path)
Run type: condensed pass (zero-tolerance + voice self-review), consistent with chapter 1's
approach for a first-draft chapter still awaiting sign-off.

## What was checked

- Em dashes and the project's banned precision-word list: caught and fixed during drafting
  (write-time hook enforced this repeatedly; final saved version is clean).
- Negative-parallelism density: reviewed every "not X" / "rather than" construction in the
  chapter. Left in place where the contrast carries substantive technical content (for
  instance, "The test statistic uses the t-distribution rather than the normal distribution"
  states a methodological fact, not a rhetorical tic; "ANOVA answers only whether at least one
  region differs, not which one" names a limitation worth stating directly). None of the
  occurrences read as padding.
- Gerund headings, paragraph-terminal summaries, staccato stacks, demonstrative labeling: none
  found on review.
- Running example continuity: the checkout-API caching-layer scenario from Chapter 1 carries
  through every section where it fits (hypothesis statement, Z/t-test, p-value, Type I/II
  error, power, sample size, confounding, paired/unpaired, ANOVA, ANCOVA, design of
  experiments); chi-squared and non-parametric sections use it directly (error-type
  distribution, skewed latency data respectively).
- Citation accuracy: the one direct quote (ASA statement on p-values) was checked against the
  source PDF and matches verbatim; see the chapter's plagiarism report.

## Verdict

Clean. No outstanding AI-pattern issues.

## Addendum: visual QA and storytelling-gap pass

Re-checked all 4 existing figures against the code that generates them and against a live
browser render of each `_generated/*.html` file: t-vs-normal, Type I/II error trade-off, power
curve, and the parametric-vs-nonparametric comparison all render cleanly, with correct titles,
axes, and slider ranges. No bugs found.

Storytelling gap: ANOVA and chi-squared/goodness-of-fit are two of this chapter's biggest topics
(a full section each) and had zero visual support, unlike every other major test family in the
chapter. Added `chapter-02-fig-anova-between-within`, box plots for three regions' latency with
a slider on the gap between region means, showing the `scipy.stats.f_oneway` F-statistic and
p-value move together as that gap widens while within-group spread stays fixed. Embedded it in
the ANOVA section. Chi-squared/goodness-of-fit was considered but left without a new figure: the
section's worked scenario (error-type counts by region) is carried adequately by the existing
prose and formula, and a contingency-table heatmap felt like padding rather than a meaningful
gap.

## Addendum: what/why/how + rigor audit (2026-09-01)

Read the full chapter against two criteria: narrative completeness (every major concept states
what it is, why it matters, and how it is computed or decided) and claim rigor (no overclaim, no
hidden limitation, no disputed point stated as settled). Most sections carry all three beats and
were left untouched, including the p-value section, whose treatment of the "probability the null
is true" misreading, backed by the ASA citation, sets the honest-limitations model the rest of
the chapter follows.

Two rigor gaps found and fixed, both cases where a limitation flagged for one test went
unmentioned for its close relative:

- ANOVA section: the chapter flags unequal-variance risk for the unpaired t-test (Welch's test as
  the safer default) but said nothing about the same risk for ANOVA's F-test. Added two sentences
  noting that unequal group variances can shift the F-test's false-positive rate off its stated
  level, and pointing to Welch-corrected ANOVA or the chapter's own Kruskal-Wallis section as the
  fix.
- ANCOVA section: "isolating the deployment strategy's effect from the confound" overstated what
  the additive ANCOVA model can guarantee. Added a sentence naming the homogeneity-of-slopes
  assumption (payload size must affect latency the same way under both deployment strategies) and
  what breaks if it does not hold.

No narrative (what/why/how) gaps were found; every major concept in this chapter carries a plain
definition, a concrete cost or consequence, and a worked example or decision rule. Re-ran
`/plagiarism-check` on the full chapter after the edits; both additions restate standard,
undisputed statistical facts in the chapter's own voice, no new citation needed, verdict unchanged
at PUBLICATION-READY. See the addendum in `.plagiarism-report.md` for search verification detail.

## Addendum: 2026-09-01, plain-language intuition layer

Added fourteen short plain-language analogy paragraphs (40-60 words each), one before the
notation in every major section, so a reader without calculus or summation-notation background
can follow the idea even if not every formula. Kept to the author's explicit length constraint:
3-4 lines, roughly 40-60 words. Zero em dashes, zero banned precision words; the write-time hook
caught and forced a rephrase of a stray instance during drafting. Re-ran `/plagiarism-check`;
all fourteen new paragraphs classified CLEAR. No existing prose, formulas, figures, or headings
were removed or altered.

## Addendum: 2026-09-01, sentence-level clarity pass

Fourth pass: a close, sentence-by-sentence clarity edit of the existing prose, not a content
add. Roughly 17 sentences touched across 11 locations. Most edits split a long sentence into
two, following the same pattern as chapter 1: a colon or semicolon plus a trailing "which is
why" or "and" clause, broken into separate sentences so each states one idea. One edit fixed a
comma-heavy sentence in the ANOVA section where the Welch's-correction aside ran directly into
the main clause with no punctuation marking it as an aside, now set off in parentheses. Another
moved an "assuming the cache did nothing" clause from the middle of the p-value sentence to the
front, so the reader hits the qualifying condition before the claim it qualifies instead of
mid-sentence. No word-count reduction was a goal. Re-ran `/plagiarism-check`; the report's
addendum confirms no new matches for the rewritten phrasing. Zero em dashes, zero banned
precision words introduced. No formulas, citations, figures, or headings touched.

## Addendum: 2026-09-01, second clarity pass

Fifth pass, a higher-bar re-read on top of the sentence-level pass above: this chapter needed
little. Found and fixed three small issues. First, the section heading "Testing a single mean:
the Z-test and the T-test" capitalized "T-test" while the body spells it lowercase ("t-test") in
every other instance, so lowercased the heading to match. Second, the sentence introducing
statistical power, "...when $H_1$ is true, in other words, $1 - \beta$," stacked two commas back
to back where the rest of the chapter uses "In other words" only after a displayed formula;
replaced with a colon for a cleaner parse. Third, the confounding-variables section's closing
sentence said the same caution from Chapter 1 "applies here, in the other direction," a phrase
with no clear referent that reads as implying a causal reversal that is not there; reworded to
state directly that the same caution applies to before-after comparisons. No jargon-before-
analogy mismatches or topic-sentence mismatches found elsewhere in the chapter. Re-ran
`/plagiarism-check`; all three fixes classified CLEAR, verdict unchanged at PUBLICATION-READY.
Zero em dashes, zero banned precision words. No facts, numbers, formulas, citations, figures, or
headings touched.

## Addendum: 2026-09-01, clarity and jargon-gap edit pass

Sixth pass: a word-by-word clarity read targeting unstated jargon and formulas whose symbols
were not fully introduced. Four fixes applied, all additive:

1. Z-test/t-test section: "degrees of freedom" appeared only in the fig-t-vs-normal caption and
   was never defined in the body. Added a clause defining it where the t-distribution is first
   introduced ($n - 1$ for the one-sample case), and explained why fewer degrees of freedom means
   heavier tails than the normal curve.
2. Same section: "Central Limit Theorem" was invoked twice as a justification but never stated.
   Added a parenthetical restating what it says (a sample mean drifts toward a normal
   distribution as sample size grows, regardless of the underlying shape).
3. Pairwise post-hoc tests section: "family-wise error rate" was named as what Tukey's HSD
   controls but never defined. Added a parenthetical definition.
4. ANCOVA section: the regression formula defined $Y$, $X_1$, and $X_2$ but left $\beta_0$,
   $\beta_1$, $\beta_2$, and $\varepsilon$ unexplained, breaking the symbol-before-use convention
   the rest of the chapter follows. Added a clause naming each: intercept, per-predictor
   coefficients, and the error term.

Applied the same humanization patterns as the passes above: no em dashes, no banned precision
words (write-time hook enforced), varied sentence rhythm, terms named and defined on first use.
Ran an anti-sycophancy self-check: the degrees-of-freedom claim is scoped to "this one-sample
test" specifically rather than stated as a universal rule, since the formula differs for other
t-test variants in this chapter; the CLT and family-wise error rate definitions are standard and
uncontested; the ANCOVA symbol gloss adds no new modeling claim, only names terms that were part
of the equation from the start. No caveat was softened or dropped, and no regression found.
Re-ran `/plagiarism-check` on the four new passages; see the plagiarism report's matching
addendum. No facts, formulas, figures, citations, or headings were removed; only clarifying
language was added.

## Addendum: 2026-09-01, image/caption/paragraph/callout formatting pass

Five changes applied throughout the chapter:
1. All five figures moved to appear before the prose that discusses them, right after the
   formula/setup needed to make each chart legible.
2. All five captions rewritten to state the takeaway (e.g. the t-test's p-value swings with
   outliers "since a single extreme value can only move a rank by one position" for the
   Wilcoxon comparison) rather than only describing the axes.
3. Visually inspected the power-curve figure's PNG against its own underlying data (verified
   directly via the frame data in the browser, not just the screenshot) after an initial
   capture looked non-monotonic; confirmed the chart's data was always correct and the issue
   was a browser screenshot-timing artifact in the regeneration process, not a chart bug.
   Regenerated all five of this chapter's PNGs with a more robust capture method (forcing the
   correct default slider frame and waiting for two animation-frame paints before
   screenshotting) to rule out the same class of artifact elsewhere.
4. Every paragraph split to a 3-4 line maximum.
5. Eight `.callout-*` boxes added (tip/note/important/warning), each 1-2 sentences,
   highlighting a specific gotcha, rule of thumb, or caveat already present in the prose.

No new external claims, citations, or statistics were introduced. No `/plagiarism-check` web
sweep was re-run since nothing new needs verification, this addendum documents that reasoning
directly. Verdict unchanged: PUBLICATION-READY (local review only).

## Addendum: 2026-09-01, second placement/caption/paragraph/callout pass

Re-checked all five figures against their current PNG exports and against the surrounding
prose. Placement was correct for every figure going into this pass (each div sits at or
immediately before the paragraph that first discusses it in depth) and all five captions
named the axes and the pattern the chart makes visible, so neither figure placement nor
caption text changed this pass.

Four paragraphs that had grown back past a 4-line source limit were split at their natural
sense break, with no sentence fragmented and no clause left orphaned: the "degrees of
freedom" paragraph in the Z-test/t-test section, the Central Limit Theorem paragraph in the
same section, the goodness-of-fit definition paragraph in the chi-squared section, and the
ANCOVA opening paragraph. One five-line paragraph (the "Three standard ways to control for a
confounder" sentence in Confounding variables) was left intact: it is a single sentence
enumerating three options with bolded terms, and splitting it at the semicolons would leave
each fragment without its own subject.

Three new callout boxes were added, one each in three major sections that had none going in
(Testing a single mean, Type I and Type II errors, Designing the experiment before running
it), leaving Confounding variables and pairwise post-hoc tests without a new callout since the
closest candidate point in each was stated in the surrounding prose without needing repeating:
a t-test default rule of thumb (`.callout-tip`), the fix-alpha-first convention behind Type
I/Type II error rates (`.callout-note`), and the rule that randomization must happen before
anyone who can influence the outcome knows the assignment (`.callout-warning`). None
duplicates an existing callout's point.

Applied this chapter's established humanization patterns to all new prose: zero em dashes,
zero banned precision words (write-time hook enforced), no gerund-opener headings, no
paragraph-terminal summaries, and sentence rhythm consistent with the surrounding voice. Ran
an anti-sycophancy self-check on every edit and caught one overclaim: a first draft of the
t-test callout stated "the t-test never does worse than the Z-test," which does not hold when
the population standard deviation is known independently of the sample (the Z-test has more
power in that case); rewrote it to state only that the t-test accounts for the extra
uncertainty of estimating the standard deviation from the sample, which the Z-test ignores.
The other two callouts and all four paragraph splits were checked and found to introduce no
claim beyond what the surrounding prose supports. Verdict unchanged: PUBLICATION-READY (local
review only).

## Addendum: 2026-09-05, gedeck-enhancement depth pass

Self-directed humanize review (book mode, GENERAL/BOOK pattern set) of the ten new passages
added in this pass: the intro roadmap items 7-8, the single-mean simulate-before-formula
insert, the Cohen's d definition, the new confidence-interval section (two subsections), the
new sequential-testing section, the unpaired-t-test permutation insert, the new two-proportion
z-test section, the ANOVA and chi-squared permutation bridges, and the new
multiple-comparisons-problem section.

Checked and clean on first pass: zero em dashes, zero banned precision words (write-time hook
enforced), no curly quotes, no conjunctive openers, no filler phrases, no title-case headings,
no boldface overuse beyond first-use term definitions, running-example continuity (checkout
API, caching layer, canary release) held throughout every new passage.

Five negation-pattern instances (Pattern 9) were found and fixed on self-review, all trailing
or embedded "X is not Y" constructions that read as rhetorical rather than substantive:
- CI-for-a-mean callout: "A wider interval is not a worse interval; it is an honest one"
  rewritten to state the positive claim first: "A wider interval reports honest uncertainty,
  not a weaker result."
- Sequential-testing callout: "A live dashboard... is not, by itself, a sequential test"
  rewritten to state the positive qualifying condition first.
- Unpaired-t-test permutation passage: "a 9.8 ms gap is not the kind of thing label-shuffling
  alone tends to produce" rewritten to the direct comparative claim: "making a 9.8 ms gap far
  bigger than what label-shuffling alone tends to produce."
- Two-proportion z-test callout: a "This is why" vague-referent construction (Pattern 53) was
  restructured so the causal clause leads and the referent is named directly, removing the
  vague "This" subject.
- Multiple-comparisons section: "is this general fact, not a quirk specific to ANOVA follow-up
  testing" rewritten to a positive claim: "names this general fact and reaches far beyond ANOVA
  follow-up testing."

All five fixes preserve the original factual content; none softened a caveat or added an
unverified claim. Every other "rather than" / "instead of" construction in the new content was
reviewed against this chapter's own established exception (kept where the contrast states a
substantive methodological or definitional fact, per the addendum above) and left in place,
matching this chapter's precedent for the pre-existing prose.

Re-ran `/plagiarism-check` on the ten new passages; see the matching addendum in
`.plagiarism-report.md`. Verdict: PUBLICATION-READY (local review only). No pre-existing prose,
figures, formulas, or citations were touched in this pass.
