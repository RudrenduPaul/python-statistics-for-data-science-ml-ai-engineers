# Humanize log: chapter-bayes-bart.md

## Addendum: 2026-09-01, plain-language intuition pass

Added three short (3-4 line) plain-language analogy paragraphs before the notation-heavy
sections on the sum-of-weak-trees idea, the three priors, and Bayesian backfitting, so a reader
without a probability/MCMC background can follow the intuition even without the formulas.
Additive only, no existing content changed. No new citations, no fabricated claims. Verified no
em dashes or banned precision words in the new text.

Mode: BOOK (auto-inferred from `books-all/` path)
Run type: condensed self-review pass (zero-tolerance + voice), matching the process used on
earlier chapters in this book, proportionate to a new chapter still awaiting author review.

## What was checked

- Em dashes and the skill's standard AI-vocabulary list (Pattern 7): zero found (structurally
  guaranteed by the project's write-time hook, which blocks both before a file can save).
- Staccato stacks, demonstrative labeling, paragraph-terminal summaries, rule-of-three padding,
  vague "This" subjects: none found.
- Gerund-style headings (Pattern B1, the book's established house rule against "Understanding
  X" / "Implementing Y" section titles): 3 found and fixed.
- Negative-parallelism density ("X, not Y" trailing contrasts): 2 instances found and rewritten
  to direct positive phrasing; several other "not" constructions reviewed and left as is because
  they state a fact directly rather than construct a rhetorical contrast (e.g. "is not a model
  with a closed-form posterior," which sets up why MCMC is needed next).
- Voice: reads as one coherent textbook-authorial voice consistent with the rest of Part 3,
  connects explicitly back to Chapter 7's canary-rollback scenario throughout rather than
  introducing an unrelated example.

## Fixes applied

1. Heading "Fitting BART: Bayesian backfitting" -> "How BART is fit: Bayesian backfitting."
2. Heading "Fitting the rollback classifier with pymc-bart" -> "The rollback classifier in
   pymc-bart."
3. Heading "Using the interval itself as a decision rule" -> "The credible interval as a
   decision rule."
4. "BART's regularization lives entirely in three prior distributions, not in a depth limit or
   a pruning rule chosen by hand" -> "Three prior distributions carry BART's entire
   regularization burden, with no depth limit or hand-chosen pruning rule doing separate work."
5. "BART is not proposing a different answer to the same question, it is proposing an answer
   with a stated margin of doubt attached" -> "BART agrees with the random forest on the answer
   and attaches a stated margin of doubt to it."

## Not applied

- Contraction insertion: skipped, matching the rest of this book's formal ISLR-derived textbook
  register.
- Full 4-pass worker architecture, parallel critique pairs, URL verification subagent: skipped
  in favor of a direct self-review, matching the process used on the book's other chapters.

## Verdict

Clean. No outstanding AI-pattern issues.

## Addendum: visual QA and storytelling-gap pass

Rechecked all 4 existing figures against a live browser render. Found and fixed one bug:
`fig-mcmc-trace` was built from two unlinked random walks passed through differently-offset
tanh transforms, which kept the two simulated chains in permanently separate value bands for
the full 1000-iteration range shown. The chapter caption explicitly says the reader should
"watch both chains settle into the same region," but the chains never did, the opposite of what
a burn-in diagnostic is supposed to demonstrate. Replaced with two AR(1) mean-reverting chains
started far apart (0.10 and 0.72) with a shared target (0.4): the chains now visibly separate in
the first ~50 iterations, then visibly mix and overlap for the rest of the run, verified against
a fresh browser render before and after the fix. No caption change was needed: the caption
described the correct target behavior from the start, the figure code just was not producing it.

Found one storytelling gap: "The three priors doing the work" is 40 lines of dense, unillustrated
explanation covering the tree-structure prior, leaf-value prior, and error-variance prior, plus
a discussion of the alpha/beta/k/m default hyperparameters and what raising or lowering k does
to the fit. Added a new figure, `fig-tree-structure-prior`, showing split probability by node
depth under the default penalty (alpha=0.95), with a slider on beta so a reader can see how that
parameter controls the decay's steepness. Verified numerically before implementing (depth 0:
95% split probability, depth 3: under 6%, matching the chapter's own claim that "most trees...
stay two or three levels deep") and visually via a browser render after. Wired into the chapter
between the tree-structure-prior paragraph and the leaf-value-prior paragraph. Did not add a
second figure for the leaf-value prior itself, to avoid front-loading the section with two
figures before the reader has read past the first one.

Ran a self-directed check (em dashes, banned precision words, negation density) on all new
prose; both passed clean via the same write-time hook enforcement as the rest of this chapter.
Did not re-run the full `/plagiarism-check` skill, since no new external facts, statistics, or
citations were introduced in this pass, only corrected simulation code and one new figure whose
caption restates a formula cited to Chipman/George/McCulloch 2010 elsewhere in the chapter.

## Addendum: what/why/how narrative + claim-rigor pass (2026-09-01)

Reviewed the chapter against a what/why/how checklist, concept by concept. Narrative structure
holds up throughout: the sum-of-weak-trees framing, the three priors, Bayesian backfitting, the
`pymc-bart` fit, variable importance, and the cost/decision-rule sections each state what the
concept is before any formula, why it matters with a concrete consequence (the automated
rollback framing carries this through the whole chapter), and how it is computed or used, mostly
with a worked figure or code example. No narrative gaps found requiring a fix.

One rigor issue found and fixed: "The three priors doing the work" stated that Chipman, George,
and McCulloch's (2010) default hyperparameters, including m = 200 trees, are the values "most
implementations, `pymc-bart` included, still use as a starting point." Verified live via
WebFetch against the current `pymc-bart` API reference
(https://www.pymc.io/projects/bart/en/latest/api_reference.html) on 2026-09-01: `pymc-bart`'s
own default is `m = 50`, not 200 (alpha = 0.95, beta = 2.0 do match; k = 2 is used internally in
the leaf-scale formula but is not a separate constructor argument). The claim overstated how
closely `pymc-bart` tracks the original paper. Corrected the paragraph to say `pymc-bart`
matches Chipman, George, and McCulloch on alpha, beta, and k, but defaults to 50 trees rather
than 200, and used that correction to explain why the chapter's own code example sets `m=100`
explicitly instead of leaving it at the library default. No new citation needed: still covered
by the chapter's existing [@chipman2010bart] entry. Full verification detail logged in the
addendum to `chapter-12-bayesian-bart.plagiarism-report.md`.

Em dashes and the banned precision-word list: zero in the new text (hook-enforced at write
time). `/plagiarism-check` re-run on the full chapter for this pass, given the corrected factual
claim; see that report's addendum.

## Addendum: sentence-level clarity pass (2026-09-01)

Read the chapter word by word and edited roughly 20 sentences purely for clarity, no content
added or cut. Kinds of edits made:

- Split long compound sentences joined by semicolons or a trailing "and" clause into two shorter
  sentences: the opening BART-versus-random-forest paragraph, the leaf-value-prior variance
  explanation, the error-variance-prior sentence, the Chipman/George/McCulloch default-values
  paragraph (also broken out of a heavily nested parenthetical into two plainer sentences), the
  backfitting-cycle closing sentence, the pymc-bart code-usage paragraph, the variable-importance
  paragraph, the cost comparison paragraph, and the R-tooling paragraph.
- Fixed one ambiguous trailing pronoun ("a fit that can look overconfident if the data does not
  support it" became "...overconfident when the data does not support the claim").
- Reworked one paragraph in "The credible interval as a decision rule" so the routing outcome
  (a wide-interval deployment gets routed to a human) leads the sentence, with the explanation
  of why that interval is typically wide following as its own sentence, instead of burying the
  outcome in the middle of a single long sentence.
- Untangled one dense "agreeing on the ranking is stronger evidence" clause in the
  variable-importance section into two plainer sentences.

Every formula, citation, number (alpha = 0.95, beta = 2, k = 2, m = 50 library default versus
m = 200 paper default), and figure caption is untouched. Zero em dashes and zero banned
precision words in the new text (write-time hook enforced, verified clean).
`/plagiarism-check` re-run on the full chapter afterward; see that report's addendum for
confirmation that no new external claims were introduced.

## Addendum: second clarity pass (2026-09-01)

Second word-by-word pass, this book's densest chapter along with 10 and 11, so a lingering
parse cost here matters more. Looked for seams from the four earlier passes rather than
repeating their work. Found and fixed five issues:

1. **Ambiguous pronoun**: the opening paragraph's "how much training data resembled it" leaves
   "it" pointing at either "that particular score" or "this particular deployment" (the nearer
   noun, and the intended referent). Replaced "it" with "this deployment."
2. **Elliptical clause**: the Fourier-series analogy ended "...each contributing a small
   correction that the others could not," leaving the reader to supply the missing verb. Added
   "supply" so the clause resolves on a first read.
3. **Uncounted enumeration**: the default-hyperparameters paragraph said Chipman, George, and
   McCulloch's defaults are used "for three of the four" but named only two grouped items (a
   combined "tree-depth penalty" for alpha/beta, and k) before "the fourth" (m), so a reader has
   to mentally split alpha and beta apart to make the count of four work. Restructured the
   opening sentence to name all four hyperparameters up front (alpha, beta, k, m), so "the first
   three" and "the fourth" both refer to a visible list instead of an implied one. Same numbers,
   same citation placement.
4. **Topic sentence versus paragraph**: "with two differences that matter" was followed by one
   run-on sentence blending both differences (revisit-vs-not, and search-for-one-ensemble-vs-
   sample-a-posterior) together via a semicolon and a "because" clause, so the promised second
   difference does not read as its own point. Split into an explicit "First,... Second,..." pair.
5. **Dense trailing clause**: the variable-importance section's closing sentence used
   "uncertainty" three times in one breath ("a smaller uncertainty than the uncertainty around
   any individual prediction, but uncertainty nonetheless"). Split into two plainer sentences
   that state the comparison once each.

No other issues found; the priors section's three analogies, the backfitting explanation, the
pymc-bart code walkthrough, and the closing decision-rule section all read cleanly. No formula,
citation, number (alpha = 0.95, beta = 2, k = 2, m = 50 library default versus m = 200 paper
default), heading, or fact was touched. Zero em dashes and zero banned precision words in the
new text (write-time hook enforced, verified clean). `/plagiarism-check` re-run on the full
chapter; see that report's addendum for confirmation that no new external claims were
introduced.

## Addendum: teaching-clarity pass (2026-09-01)

Read the full chapter word by word for teaching-value gaps, not sentence-level parse cost this
time. Found and fixed three spots where a term or logical link was left for the reader to
supply on their own:

1. **Undefined formula-equivalent term**: the opening paragraph introduced "a full posterior
   distribution over the score" with no restatement, breaking this chapter's own pattern of
   restating every dense technical phrase in plain terms right after it appears. Added an "In
   other words" sentence grounding what a posterior distribution over a risk score concretely
   means for one deployment.
2. **Subject-verb mismatch**: "`pymc-bart` needs at most a glance at the tree count" put the
   software, not the person tuning it, as the one doing the glancing, which does not parse.
   Rewrote so "tuning `pymc-bart`" is the subject that needs little more than a glance, and
   named what the random forest and gradient-boosting comparisons need instead (deliberate
   search) so the contrast holds up instead of being asserted.
3. **Unexplained jargon at a load-bearing sentence**: "not a model with a closed-form
   posterior" is the sentence that motivates why BART needs MCMC at all, but the chapter never
   said what "closed-form posterior" means. Added a clause explaining it as the absence of a
   formula that converts priors and data directly into the posterior, and expanded "MCMC" to
   "MCMC (Markov chain Monte Carlo)" on its first literal use in this chapter's running text.

Ran an anti-sycophancy self-check on all three edits: none soften a technical claim into
something inaccurate, and no caveat from the surrounding text was dropped. The "closed-form
posterior" clause was checked against why BART lacks one (the combinatorial space of tree
structures, not just the priors being non-conjugate) and phrased generally enough ("there is no
formula that converts the priors and the data directly into the posterior") to stay correct
without overclaiming a specific mechanism the chapter does not otherwise cover.

Zero em dashes and zero banned precision words in the new text (write-time hook enforced; one
banned word from the list was caught and rephrased during drafting, before anything was saved).
No formula, citation, number, heading, code block, or existing fact was touched; the three edits
are additive clarifications only. `chapter-12-bayesian-bart.plagiarism-report.md` re-assessed
for this pass; see that file for the updated risk classification.

## Addendum: figure-reordering and structure pass (2026-09-01)

Five-part formatting pass applied to the whole chapter.

**1. Figure placement**: all five figure divs (fig-ensemble-buildup, fig-tree-structure-prior,
fig-mcmc-trace, fig-bart-vs-rf-interval, fig-variable-importance) moved to sit right after their
section heading, preceded by one short orienting sentence, instead of after two or more
paragraphs of setup prose. The sentence that used to name each figure by its `@fig-` label before
the reader had seen it was rewritten to follow the figure instead, describing what was just
shown.

**2. Captions**: three rewritten to state the takeaway rather than just the axes
(fig-ensemble-buildup, fig-bart-vs-rf-interval, fig-variable-importance). Two kept as written,
since they stated a takeaway from the start (fig-tree-structure-prior, fig-mcmc-trace).

**3. Visual inspection**: reopened all five PNGs and checked each against the surrounding prose
a second time. No mismatch found. fig-mcmc-trace was rechecked with particular care, since this
is the figure the visual QA pass fixed earlier in this log; it continues to show two chains
wandering separately during burn-in, then settling into overlapping variation, matching the
prose.

**4. Paragraph length**: roughly a dozen paragraphs split at natural breaks, including the
opening hook paragraph, the leaf-value-prior explanation, the Chipman/George/McCulloch
defaults paragraph, the backfitting-mechanics paragraph, the cost-comparison paragraphs, and the
closing decision-rule paragraph. No sentence was cut mid-thought; each split fell at an existing
sentence boundary.

**5. Callouts**: two added. A `.callout-note` in "The three priors doing the work" flags the
`m=50`-library-default-versus-`m=200`-paper-default gap this log has tracked since an earlier
pass as a corrected claim. A `.callout-warning` in "What this costs, and what it is worth" flags
that BART does not parallelize within a single MCMC chain the way a random forest's independent
trees do.

No formula, citation, number, or fact was changed anywhere in this pass. Zero em dashes and zero
banned precision words in the new text (write-time hook enforced). `/plagiarism-check` re-run
on the full chapter; see that report's addendum for this pass, since the new prose restates
verified facts from earlier passes and introduces no new external claim.

## Addendum: caption accuracy fixes, paragraph splits, four new callouts (2026-09-01)

Re-read the full chapter against every figure PNG directly. Figure placement was correct going
in for every section (each div sits right after its heading and before the paragraph that
discusses it in depth), so no figure moved this pass. Two accuracy problems were found while
checking captions against the PNGs, both fixed in the body prose next to the affected figure:

1. **fig-tree-structure-prior mismatch**: the paragraph right after the figure said the split
   probability falls "less than a one-in-five chance" by depth 3, describing the figure as
   plotted "under the default penalty." The static PNG's slider sits at beta=1 (visible in its
   own label), where depth 3 is 0.24, not under 0.2. The paper/library default of beta=2
   (named later in the same section) gives roughly 0.06 at depth 3, which is under one-in-five,
   so the original sentence was describing the wrong slider position. Rewrote the sentence to
   state the beta=1 value correctly (fallen under one-in-four) and added the beta=2 comparison
   (drops under one-in-ten) as a second sentence, so both numbers in the text now match what a
   reader would compute at each named slider position.
2. **fig-bart-vs-rf-interval mismatch**: the paragraph after the figure claimed intervals stay
   tight for deployments "near the middle of the training data" and widen "toward the edge of
   what the model has seen." Reading the PNG directly, the five lowest-probability deployments
   carry the widest intervals (several reaching to 0), and deployment 4, the highest-probability
   one, carries the narrowest. That is the reverse of a simple middle-versus-edge story, and this
   chapter's chart does not show each deployment's position in the four-feature training-data
   distribution, so the middle/edge explanation cannot be confirmed from this figure. Rewrote the
   paragraph to state only what is checkable: interval width does not track the point estimate,
   and this set's widest intervals sit on its lowest-probability deployments. No new causal claim
   was added in its place.

Caption rewrites (4 of 5, after re-reading each PNG): fig-ensemble-buildup, fig-tree-structure-
prior, fig-mcmc-trace, and fig-bart-vs-rf-interval now name their axes and describe what the
slider's starting position shows, with specific values read off the chart rather than a general
description of the whole slider range. fig-variable-importance's caption was extended to name
its axes; its existing takeaway sentence held up on inspection and was kept.

Paragraph splitting: split roughly 20 over-length paragraphs at sentence boundaries across the
chapter, including the random-forest-versus-BART variance paragraph, the Chipman/George/
McCulloch defaults paragraph, the backfitting-cycle paragraph, the R-tooling paragraph, and both
paragraphs in the closing decision-rule section. Checked each split for an orphaned one-sentence
paragraph; two early drafts left a single trailing sentence standing alone ("Fall back to paging
an on-call engineer when it is not," and "The cycle then repeats,") and both were folded back
into the sentence before them instead.

Callouts (4 added): a `.callout-tip` in "A sum of weak trees" on adding trees instead of
deepening them, a `.callout-note` in "The rollback classifier in pymc-bart" on checking
convergence diagnostics before trusting a credible interval, a `.callout-tip` in "Variable
importance from the posterior" on treating overlapping importance boxes as an unsettled ranking,
and a `.callout-important` in "The credible interval as a decision rule" on setting the interval
threshold before looking at any specific deployment's score. None repeats a point covered by one
of the chapter's five existing callouts, and no section picked up more than one new callout.

Anti-sycophancy self-check: re-read both corrected paragraphs (tree-structure-prior probability,
interval-width pattern) against the PNGs a second time after rewriting, to confirm the fix did
not introduce a new unverified claim in place of the old one. The interval-width paragraph in
particular was checked for the temptation to supply a causal explanation (why the pattern runs
this way) and deliberately left without one, since the chart does not carry that information. One
draft callout used the "X is not Y; it is Z" two-sentence tic flagged earlier in this log
(Addendum: 2026-09-01, plain-language intuition pass); rewritten into a single sentence before
saving.

Zero em dashes and zero banned precision words in the new text, checked with a direct grep of
the whole file after every edit. `/plagiarism-check` re-run on the full chapter; see that
report's matching addendum.

## Addendum: third callout added for coverage parity (2026-09-01)

Two callouts against a roughly 2,500-word chapter sat below the one-per-400-to-600-word target
this pass was aiming for, and below the three each that Chapter 10 and Chapter 11 reached. Added
a third, a `.callout-tip` after the R-hat convergence-diagnostic paragraph closing "How BART is
fit," pointing out that R-hat needs checking across every parameter in the ensemble, not just the
single leaf-value trace the chapter walks through by hand. The callout restates a claim the
surrounding paragraph and this chapter's plagiarism report cover, so no new fact or citation was
introduced. Zero em dashes and zero banned precision words in the new sentence (write-time hook
enforced; two words were caught and rephrased during drafting of this addendum, before saving).
`/plagiarism-check` re-run; see that report's addendum for this specific callout.

## Addendum: light-touch gap pass, sigmoid-link explanation added (2026-09-05)

A cross-chapter audit found one specific, unaddressed gap in this otherwise strong chapter: the
`pymc-bart` code example wraps `mu` in `pm.math.sigmoid(mu)` with no explanation of why that step
is needed, unlike Chapter 4's careful treatment of the same logistic transform for classical
logistic regression. Added a 243-word passage in "The rollback classifier in pymc-bart," right
after the `X_train`/`m=100` explanatory paragraph and before the `tune=1000` callout: a short code
snippet computing `sigmoid` at five representative `mu` values, then what the sigmoid does, why
skipping it would misroute a downstream decision, and a closing cross-reference to Chapter 4's
logistic function with the formula itself. Figure/code-output-first, What/Why/How ordering held
throughout, matching this chapter's established pattern.

Checked this pass:

- Em dashes and the project's standard banned precision-word list: zero found, confirmed by a
  direct grep of the new passage in addition to the write-time hook.
- Gerund-style headings: none added; the new passage carries no heading of its own, matching this
  chapter's flat (no `###`) section structure.
- Negative-parallelism density ("X, not Y" trailing contrasts): none introduced.
- Voice: reads as a continuation of the surrounding pymc-bart walkthrough, not a bolted-on aside;
  keeps the running canary-rollback example rather than introducing a new one.
- Numbers: the five sigmoid output values were computed directly with `numpy` during drafting
  (`[0.12, 0.38, 0.50, 0.62, 0.88]` for `mu = [-2.0, -0.5, 0.0, 0.5, 2.0]`), not estimated.
- Cross-reference accuracy: re-read Chapter 4's logistic-function section directly to confirm the
  formula and framing cited here match what that chapter states.

No contraction insertion applied, matching the rest of this book's formal register. No existing
sentence, figure, callout, or citation was touched.

## Verdict

Clean. No outstanding AI-pattern issues in the new passage.
