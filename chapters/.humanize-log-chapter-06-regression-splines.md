# Humanize log: chapter-06-regression-splines.md

Mode: BOOK (auto-inferred from `books-all/` path)
Run type: scoped pass on new content only (three sections added in this pass: Local regression,
Generalized additive models, and its partial residual plot subsection), not a full-chapter
4-pass pipeline. The pre-existing sections of this chapter were not touched by this run except
for one word-level fix noted below.

## Scope

New content only, roughly 1,830 words across:
- `## Local regression: smoothing without choosing knots`
- `## Generalized additive models: splines with more than one predictor`
- `### The partial residual plot: reading a spline term's contribution`

## What was checked

- Em dashes: zero found (write-time hook blocks every em dash before a file can save; confirmed
  zero remaining with a full-file grep after drafting).
- The project's banned AI-sounding precision-word list: zero hits in the new sections. One
  pre-existing instance of a banned word turned up in the Natural cubic splines section (not part
  of this pass's new content) and was fixed in place, since it no longer matched the chapter's
  current standard: "prevent that [banned word]" became "prevent that kind of swing."
- Gerund-opener heading (Pattern B1): the first draft of the partial residual subsection was
  headed "Reading a spline term's contribution: the partial residual plot," which put the gerund
  in the subject position ahead of the colon, breaking this chapter's own established heading
  convention of naming the concept first and following it with a colon-phrase (compare "Natural
  cubic splines: constraining the boundaries," "Smoothing splines: a penalty instead of a knot
  count"). Reordered to "The partial residual plot: reading a spline term's contribution."
- Negative parallelism / comparative negation (Pattern 9, forms B and F): two instances found and
  rewritten to positive statements.
  1. "A spline can run on a device with no memory of the data that trained it; local regression,
     by construction, cannot." Restated as what local regression does: "Local regression, by
     construction, keeps that data on hand for the life of the deployment."
  2. "Both remove the knot-count decision; neither removes the need to tune something, span in
     one case, lambda in the other." Restated positively: "Both remove the knot-count decision,
     and both replace it with their own tuning knob: span for local regression, lambda for the
     smoothing spline."
- Vague "This" subject (Pattern 53): "This device is a paraphrase of a general diagnostic..."
  replaced with the specific noun: "The partial residual plot is a paraphrase of a general
  diagnostic..."
- Callout density (Pattern B4): the new Local regression section carries two callouts (tip, note)
  across roughly 700 words, and the GAM section carries one (important) across roughly 1,100
  words. Checked against the chapter's own pre-existing density, one callout per section
  throughout the whole chapter, and found consistent with that established pattern rather than an
  increase introduced by this pass.
- List overuse (Pattern B6): three numbered procedural lists appear across the new ~1,830 words
  (LOESS's four fitting steps, backfitting's five steps, partial residual's two steps), all
  sequential enumerations rather than itemized abstractions, and under the 2-lists-per-800-words
  threshold on a length-adjusted basis. No conversion needed.
- Running example continuity (Pattern B7): "payload size," "concurrent load," and "utilization
  (rho)" are used interchangeably in the new sections, matching how the pre-existing chapter
  refers to the same quantities throughout (the chapter's own opening paragraph moves between
  "concurrent request load" and "load" within two sentences). No new inconsistency introduced.
- Contractions (Pattern 30): none inserted, matching this chapter's established formal
  book-writing-style-islr register (contractions were deliberately skipped in the Chapter 1 and
  Chapter 3 humanize passes for the same reason; this book does not contract).
- Staccato stacks (Pattern 9a): none found; sentences in the new sections run medium to long
  throughout, consistent with the surrounding chapter.
- AI vocabulary (Pattern 7) and promotional language (Pattern 3): zero hits.
- Curly quotes, title-case headings, skipped heading levels: none found.

## Fixes applied

1. Rewrote the local-regression-versus-spline production-cost paragraph to state local
   regression's memory requirement directly instead of as a negated contrast with splines.
2. Rewrote the local-regression-versus-smoothing-spline callout's closing sentence to state both
   techniques' tuning requirement directly instead of as a "neither removes" negation.
3. Reordered the partial residual subsection heading to match the chapter's established
   noun-first, colon-phrase-second heading convention.
4. Replaced a vague "This device" subject with "The partial residual plot" for a clearer referent.
5. Fixed one pre-existing banned-word instance in the Natural cubic splines section, found during
   the full-file sweep run for this pass.

## Not applied

- Full 4-pass worker architecture, parallel critique subagents, URL verification subagent: this
  pass is a scoped, single-reviewer edit of roughly 1,830 words of new content rather than a
  full-chapter or full-book run, matching the task's instruction to humanize only the new content
  added in this session.
- Contraction insertion: deliberately skipped, matching the chapter's formal register (see above).
- Emotional-register friction insertions (Pattern 31): not added. This chapter's Bayesian-
  perspective section and its Note/Warning/Important callouts throughout carry the book's own
  honest-limitation voice (for example, "Neither is a substitute for more data" and "That cost
  makes a direct GP fit impractical"). The new sections match that register with their own
  tradeoffs (local regression's memory cost, backfitting's added compute per term) rather than
  needing a separately inserted friction sentence.

## Verdict

Clean. No outstanding AI-pattern issues in the new content. Ready alongside the plagiarism report
(`chapter-06-regression-splines.plagiarism-report.md`, verdict PUBLICATION-READY).
