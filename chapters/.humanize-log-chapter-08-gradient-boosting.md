# Humanize log: chapter-boosting-gradient-boosting.md

Mode: BOOK (auto-inferred from `books-all/` path)
Run type: condensed self-review (zero-tolerance + voice), matching the approach used on
chapters 1-5. First-draft chapter awaiting author review, not a pre-submission run.

## What was checked

- Em dashes: zero (structurally guaranteed by the project's write-time hook, which also
  blocked several drafts of this file until banned precision words were removed).
- The skill's standard AI-vocabulary list: zero hits.
- Staccato stacks, paragraph-terminal summaries, rule-of-three padding, vague "This" subjects:
  none found.
- Demonstrative labeling: one instance caught by the write-time hook itself during drafting
  ("this is the trade-off... :") and rewritten before the file saved.
- Negative-parallelism density: low. A few single instances ("is not simply a fast
  implementation... it changes", "does not have as natural a Bayesian formulation", "It is
  not, and LightGBM is explicit about the trade-off") were left as-is: each states a contrast
  the chapter needs (what XGBoost is versus what it merely resembles, what Bayesian boosting
  lacks versus BART/linear regression, a direct answer to a rhetorical question in the same
  "Does it make sense...? It does not" pattern chapter 1 established as house style), not a
  rhetorical tic repeated for padding.
- Gerund heading: one found and fixed. "Choosing among gradient boosting, LightGBM, and a
  random forest" -> "Random forest, gradient boosting, or LightGBM" (Pattern B1).
- Voice: reads as one coherent textbook-authorial voice, consistent with the register the
  earlier chapters set, with the same sentence-length variety (long analytical sentences
  broken by short direct ones, e.g. "Two changes account for most of its practical
  advantage.").

## Fixes applied

1. Heading "Choosing among gradient boosting, LightGBM, and a random forest" -> "Random
   forest, gradient boosting, or LightGBM" (gerund opener, Pattern B1).
2. One demonstrative-labeling construction was caught and rewritten during drafting itself,
   not as a separate post-hoc pass.

## Not applied

- Contraction insertion (Pattern 30): deliberately skipped, matching every other chapter's
  choice to hold the formal ISLR textbook register rather than contract.
- Full 4-pass worker architecture, parallel critique pairs, URL verification subagent: skipped
  in favor of a direct self-review, proportionate to a first-draft chapter still awaiting
  author sign-off, consistent with how chapters 1-5 were handled.

## Verdict

Clean. No outstanding AI-pattern issues after the one heading fix. Ready for author review
alongside the plagiarism report.

## Addendum: visual QA and storytelling-gap pass

Rechecked all 4 existing figures against a live browser render of each `_generated/*.html` file.
All rendered correctly (clean loss curves, a correct leaf-wise-vs-level-wise comparison, a
sensible GP hyperparameter surface). As a precaution, applied the same slider-safety fix
discovered while auditing chapter 7 (Plotly's frame/animate transitions can leave a stray
diagonal artifact on a line trace; added explicit `redraw: true` and `transition: {duration: 0}`
to every animate step in this file's two slider figures) even though live testing did not
reproduce a visible defect here.

Filled one storytelling gap: the "Boosting versus bagging" section describes the sequential
fit-a-tree-to-the-residual mechanism in prose but had no figure showing it happen. Added
`fig_boosting_rounds`, a `GradientBoostingRegressor` fit with `staged_predict` exposed at 1, 5,
20, and 100 rounds via a visibility-toggle slider (not frame/animate, per the chapter 7 finding),
so the reader watches the running prediction go from a single shallow split to a close trace of
the underlying curve. Verified all four checkpoints render correctly, including round 100 via a
direct Plotly API call. New prose self-reviewed against the same AI-pattern checklist above
(clean, no new issues).

## Addendum: 2026-09-01, plain-language intuition layer

Added 3 short (40-60 word) plain-language analogies before formula-heavy concept
introductions, per the author's request that every major concept get an intuitive paragraph a
high-school reader could follow before the notation: the negative gradient (walking downhill in
fog), XGBoost's second-order Newton boosting (a driver watching both slope and curve sharpness),
and Bayesian hyperparameter optimization (a treasure hunter learning where to dig next). All
three are original phrasings of standard pedagogical analogies, not reproductions of any single
source's wording, so no new plagiarism-check pass was run; no new citations or factual claims
were introduced. Verified zero em dashes across the file after editing.

## Addendum: 2026-09-01, sentence-by-sentence clarity pass

Fourth-pass clarity edit, distinct from the content passes above: no new facts, examples,
figures, or sections were added; every change reworded or restructured a sentence that was
sitting in the chapter going into this pass. Touched roughly 20 sentences across the chapter.
Kinds of edits made, with counts:

- **Long-sentence splits**: about 11 instances, concentrated in the XGBoost regularization and
  missing-values paragraph (which had buried "why XGBoost won every competition" under a single
  75-word sentence), the LightGBM histogram-splitting and GOSS descriptions, and the closing
  "Where Part 2 leaves off" section.
- **Voice-consistency fixes**: two spots where a bare imperative step-list ("Add the new tree's
  predictions..."; "fit the next tree...") had been dropped into otherwise third-person
  descriptive prose describing the boosting algorithm; rewritten so the algorithm stays the
  grammatical subject throughout.
- **Paragraph restructuring for scannability**: the four-question comparison paragraph (dataset
  size, tuning time, interpretability, overfitting risk) was one dense block running each
  question and answer together; split into four short paragraphs, one question per paragraph, no
  content added or removed.
- **Forward-reference fix**: the Bayesian-hyperparameter-optimization analogy ("this approach
  learns from every trial...") originally appeared before the term it was illustrating was named;
  reordered so the term comes first and the analogy follows it.
- **Redundant-phrasing trim**: tightened the histogram-binning sentence, which restated "split
  precision" and "what the bucketing gives up" as two separate clauses for the same idea.

Re-ran `/plagiarism-check` on the full chapter after these edits; results appended to
`chapter-08-gradient-boosting.plagiarism-report.md` (Addendum, 2026-09-01). No new critical,
warning, or note-level findings; the three existing citations were untouched. Verified zero em
dashes and zero banned precision words across the file after editing.

## Addendum: 2026-09-01, second clarity pass

Fifth-pass clarity edit, run specifically to catch what the fourth pass above missed. This
chapter needed comparatively little; two small fixes, both voice/terminology consistency issues
rather than parsing problems:

1. **Pronoun voice break**: the walking-downhill-in-fog analogy opening the gradient-boosting
   section used second-person "you" ("you cannot see the bottom, but you can feel..."), while
   every other analogy in this chapter (the driver steering downhill for Newton boosting, the
   treasure hunter for Bayesian hyperparameter search) and in Chapters 7 and 9 uses third-person
   framing. This was more noticeable than it would otherwise be because the downhill-fog and
   driver analogies are a deliberate paired progression (first-order gradient, then
   second-order curvature), so the voice mismatch sat between two analogies clearly meant to
   echo each other. Rewrote to a third-person hiker.
2. **Term-introduction inconsistency**: "negative gradient" was introduced in quotation marks
   rather than italics, the only defined term in this chapter (or Chapter 7) not italicized on
   first use. Changed to italics to match house style.

No new facts, examples, figures, or sections were added or removed. Re-ran `/plagiarism-check`;
results appended to `chapter-08-gradient-boosting.plagiarism-report.md` (Addendum, 2026-09-01,
second clarity pass). No new critical, warning, or note-level findings. Verified zero em dashes
and zero banned precision words across the file after editing.

## Addendum: 2026-09-01, teaching-clarity edit pass

Sixth-pass edit, word-by-word read for jargon left unstated and abstract claims with no concrete
backing. No section was restructured and no technical content was removed. Four fixes:

1. **Vague, unsupported claim (log-loss gradient)**: "the gradient is a different, generally more
   informative quantity" told the reader nothing about what that quantity is, unlike the
   squared-error case two sentences earlier, which spells out the residual in full. Rewrote to
   give the log-loss negative gradient in the same "true value minus prediction" shape as the
   squared-error case, on a probability scale.
2. **Unexpanded acronym (CART)**: "the classic CART approach" never says what CART stands for,
   the only unexpanded acronym in this chapter or Chapter 7. Expanded it in place
   (Classification And Regression Trees).
3. **Abstract claim, no concrete grounding (GOSS upweighting)**: "(upweighted to correct the bias
   this introduces)" named a mechanism without saying what it does. Added a clause explaining that
   upweighting means each kept small-gradient observation counts extra in the loss calculation.
4. **Unstated jargon (SHAP-value)**: introduced by name with no gloss, the only defined technical
   term in the chapter's interpretability discussion left unexplained. Added a short parenthetical
   describing what a SHAP value does (splits credit for one prediction among its input features).

Anti-sycophancy self-check caught a mistake in fix 1 before it shipped: the first draft of the
log-loss rewrite read "the predicted probability minus the true outcome," which is the sign of
the (positive) gradient, not the negative gradient the surrounding paragraph says the tree is
fit to. Re-derived the log-loss gradient by hand (dL/df = p - y for the standard binary
cross-entropy parameterization, so the negative gradient is y - p) and corrected the sentence to
"the true outcome minus the predicted probability," matching both the math and the parallel
structure of the squared-error sentence next to it. This is the kind of regression the
self-check step is meant to catch: a phrasing that reads more smoothly but states the wrong
sign. No other caveat, sign, or technical claim in this pass was found to be incorrect on
review; the CART, GOSS, and SHAP additions were checked against their standard definitions and
match.

Re-ran `/plagiarism-check` on the full chapter after these edits; results appended to
`chapter-08-gradient-boosting.plagiarism-report.md` (Addendum, 2026-09-01, teaching-clarity
pass). No new critical, warning, or note-level findings. Verified zero em dashes and zero banned
precision words across the file after editing.

## Addendum: 2026-09-01, image/caption/paragraph/callout pass

Figure order: 4 of 5 figures had their prose introduction ("@fig-slug shows...") appearing
before the figure div, the old convention from an earlier authoring pass. Moved
fig-shrinkage-early-stopping, fig-leafwise-vs-levelwise, fig-model-comparison-size, and
fig-gp-hyperparameter-search so the image now appears first, with all naming and interpretive
prose following. Only fig-boosting-rounds had the right order from the start.

Captions rewritten to state a takeaway: shrinkage/early-stopping (high rate overfits and
reverses), leafwise/levelwise (leaf-wise reaches lower loss with fewer leaves), model
comparison (boosting's edge widens with more data), GP search (how the next trial gets
picked).

Visual inspection: checked `chapter-boosting-fig-leafwise-vs-levelwise.png` against its prose
claim (leaf-wise beats level-wise at a fixed leaf budget) and confirmed it matches. No
mismatches found in this chapter.

Split roughly 8 of the longest paragraphs (7+ lines), including the opening, boosting-vs-bagging
mechanism, the negative-gradient explanation, XGBoost's split-finding paragraph, GOSS, and the
closing Part 2 summary.

Added 3 callouts: a tip on prioritizing early stopping over learning-rate tuning, a note that
LightGBM's speed comes from splitting and growth strategy rather than a different core
algorithm, and an important-flag on Bayesian boosting's research-stage maturity.

No formulas, citations, or facts altered.

Re-ran `/plagiarism-check` on the full chapter after these edits; results appended to
`chapter-08-gradient-boosting.plagiarism-report.md` (Addendum, 2026-09-01, figure-first
structure and paragraph-formatting pass re-check). No new critical, warning, or note-level
findings. Verified zero em dashes and zero banned precision words across the file after editing.

## Addendum: 2026-09-01, second image/caption/paragraph/callout pass

Second pass over the same checklist, run against a stricter 4-source-line paragraph ceiling
than the prior pass used (that pass split paragraphs at 7+ lines; this pass targets any
paragraph over about 4 lines). Checked all 5 figures against a fresh render of each PNG.

**Figure placement**: all 5 figures sat in the correct position going into this pass, image
before the paragraph that discusses it in depth. No moves needed this pass.

**Caption accuracy check against the PNGs**: 4 of 5 captions held up against the image. One did
not. `fig-model-comparison-size`'s caption read "Boosting's edge over the forest widens as more
data arrives," which implies boosting leads from the start. The PNG shows the opposite at the
smallest training size: the random forest has lower validation error there, and the two lines
cross before boosting pulls ahead. Rewrote the caption to state the crossover, and adjusted the
one sentence of body prose directly following the figure that repeated the same "close together
at small sizes" framing, so the prose and the corrected caption agree with the chart and with
each other. No other caption changed.

**Paragraphs split**: 14 paragraphs stood over 4 source lines going into this pass (the prior
pass's 7-line threshold had left them intact): the Friedman/negative-gradient paragraph, the
fig-boosting-rounds interpretation paragraph, the shrinkage paragraph, the trees-and-
learning-rate paragraph, the Tree depth section's single 8-line paragraph (split into three), the
XGBoost regularization paragraph, the Newton-boosting analogy-plus-explanation paragraph (split
into three), the histogram-split-finding paragraph, the leaf-wise-growth paragraph, the
"does leaf-wise strictly improve" paragraph, the Bayesian-hyperparameter-optimization paragraph
(split into three), the "in other words" posterior-decision paragraph, the second-thread
paragraph, and the closing Part 2 summary paragraph. Each split at a sentence boundary present
in the source; no sentence was cut mid-clause and no paragraph was left as a single orphaned
fragment shorter than one full sentence.

**Callouts added**: 4, one per major section that had none going into this pass (max one new
callout per section, per the task's own limit):

- Boosting versus bagging: a note that boosted trees cannot train in parallel across trees the
  way a random forest's trees can, since each tree depends on the finished output of the one
  before it. This restates, in callout form, the sequential-dependency fact the surrounding
  paragraph states; it is not a new claim.
- Tree depth as a regularizer: a note flagging that carrying a random forest's deep-tree habit
  into a boosted model is a common tuning mistake, since the two families want opposite depth
  choices.
- XGBoost: a tip that native missing-value handling means skipping the imputation step for this
  model, restating what the body paragraph directly above it states.
- Random forest, gradient boosting, or LightGBM: a note that more data favors boosting only past
  a certain point, and that a random forest is the safer small-dataset default, synthesizing the
  crossover shown in the (now corrected) figure and the small-dataset caution stated later in the
  same section's fourth question.

No callout was added to LightGBM or A Bayesian perspective, since both sections carry a
callout from the prior pass and the one-new-callout-per-section limit does not authorize
stacking a second one where the existing one covers the section's main practical point.

**New-text humanization patterns applied**: matched the chapter's register throughout, no
contractions (per the chapter's standing choice to hold the formal textbook voice), varied
sentence length inside each callout (a short direct claim followed by a longer explanatory
clause, mirroring the pattern logged for this chapter's body prose), no vague demonstrative
subjects, no rule-of-three padding, no paragraph-terminal summary sentences. Zero em dashes and
zero banned precision words verified across the full file after editing.

**Anti-sycophancy self-check**: re-read every split and the caption/prose rewrite against the
source text and the PNG. The `fig-model-comparison-size` caption and its adjoining prose
sentence were the only substantive correction; both were re-checked against the image a second
time before finishing to confirm the corrected direction (forest better at the smallest size,
boosting ahead after the crossover) matches what the chart shows, not an assumption. No caveat
present in the original text was dropped by any paragraph split; each split preserved the
original wording, only inserting a paragraph break at an existing sentence boundary.

Re-ran `/plagiarism-check` on the full chapter after these edits; results appended to
`chapter-08-gradient-boosting.plagiarism-report.md` (Addendum, 2026-09-01, second
image/caption/paragraph/callout pass). No new critical, warning, or note-level findings.

## Addendum: 2026-09-05, gedeck-enhancement pass (published-repo added as authoritative copy)

This file previously existed only under `quarto-book/chapters/`, tracking a chapter slug from
an earlier authoring pass (`chapter-boosting-gradient-boosting.md`). Per the project's
divergence-resolution decision, `published-repo/chapters/` is now the authoritative edit
target, so this file is copied here going forward, with this addendum appended for the new
work done in this session. `published-repo/chapters/chapter-08-gradient-boosting.plagiarism-
report.md` was created fresh in this session (it did not exist in `published-repo/` before)
and carries the base-content audit history forward from `quarto-book`'s report under its own
"Prior audit history" section.

**What was added**: (1) a short cross-reference passage in "Boosting versus bagging" (two new
paragraphs after the log-loss discussion) pointing to Chapter 7's `@fig-confusion-threshold`
section for the confusion matrix, precision, recall, F1, and ROC-AUC treatment, instead of
rebuilding it for a boosted classifier; (2) a new section, "Regularized versus unregularized:
an overfitting curve," between "Tree depth as a regularizer" and "XGBoost," with a new figure
(`fig-overfitting-curve`) comparing an unregularized gradient boosting configuration (high
learning rate, deep trees) against a regularized one (low learning rate, shallow trees) on a
new simulated dataset (production API records: queue depth, cache-miss rate, retry count,
payload size, predicting response latency), built only for this comparison and distinct from
the `rollback_risk_data` dataset used elsewhere in the chapter and from gedeck's lending and
S&P 500 datasets.

**Condensed self-review** (matching the approach used on this chapter's earlier passes, rather
than the full multi-agent pipeline, given the scope: two additions totaling about 500 words):

- Em dashes: zero, confirmed by direct search across the full file after editing (also
  structurally guaranteed by the project's write-time hook).
- Banned precision words, per the project's standing word-ban list: zero, confirmed by direct
  search.
- Negative parallelism (Pattern 9, all forms): five instances found and fixed on first review,
  all in the new content: a trailing "X, not Y" construction in the cross-reference passage
  (rewritten as two direct statements), an "X rather than Y" construction describing the
  figure's trajectory (rewritten to state the positive claim directly), a trailing "not signal
  that will hold up" construction (restructured into two sentences), an "instead of near the
  beginning" exclusion clause (dropped, the positive claim alone carries the point), and a
  trailing "instead of one at a time" construction closing the how-to-compute paragraph
  (rewritten as a direct positive statement). Pre-existing "rather than"/"instead of"
  instances elsewhere in the chapter were left untouched, out of this session's scope and
  reviewed and accepted as house style in an earlier pass.
- Demonstrative labeling (Pattern 9b): one instance found and fixed, "That gap is the concrete
  production risk regularization guards against" rewritten to "Regularization guards against a
  concrete production risk here," making the label's subject the direct grammatical subject.
- Staccato stacks, paragraph-terminal summaries, rule-of-three padding, vague "This" subjects,
  conjunctive openers, filler phrases: none found in the new content.
- Rhythm and voice: both additions use the chapter's established techniques, colon pivots (the
  cross-reference passage's "threshold by threshold. Gradient boosting changes... :"),
  parenthetical asides (the configuration descriptions in the new figure caption), and the
  same no-contraction formal register the rest of the chapter holds throughout.
- Heading check: "Regularized versus unregularized: an overfitting curve" is a noun-phrase
  heading with no gerund opener, consistent with every other heading in this chapter.

**Verdict**: clean after the five negation fixes and one demonstrative-labeling fix listed
above. Both additions match the chapter's established voice.
