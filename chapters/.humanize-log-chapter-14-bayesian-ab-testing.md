# Humanize log: chapter-05-bayesian-ab-testing.md

Mode: BOOK (auto-inferred from `books-all/` path)
Run type: condensed pass (self-review), matching chapter 1's process. This is a first-draft
chapter awaiting author review.

## What was checked

- Em dashes: zero found (write-time hook also blocks these).
- The skill's standard AI-vocabulary list: zero hits.
- Staccato stacks, vague "This" subjects, paragraph-terminal summaries: none found.
- Two section headings used gerund/negation forms and were rewritten to noun-phrase claims:
  "Choosing a prior" -> "Priors: how much to assume before the data arrives"; "What Bayesian
  inference does not fix" -> "The limits of Bayesian inference".
- One negation-ending closing sentence rewritten positively: "They do not substitute for
  designing the experiment correctly in the first place" -> "Designing the experiment correctly
  still comes first."
- Remaining single-clause negations ("Stronger priors are not automatically better," "The
  disagreement is not a contradiction," "not convention alone") were kept: each is followed by
  the positive explanation that resolves it, matching the ISLR "does it make sense" pattern
  rather than a decorative AI contrast tic.
- All numeric claims (posterior means, win probabilities, expected loss, z-test statistics)
  traced back to a companion script run in this session; none fabricated.

## Post-expansion note

Two sections were added after the initial draft to bring the chapter to appropriate depth:
"Continuous monitoring and the peeking problem" (citing Georgiev 2017's rebuttal of the "Bayesian
peeking is free" claim, an honest counterweight so the chapter is not one-sided) and "Beyond
conversion rates" (Normal-Normal conjugacy for continuous metrics, with a fully verified worked
example, and an honest note on where this book's `pymc`/`arviz` dependencies become necessary).
Both were checked against the same pattern list; one double-negation sentence in the peeking
section was tightened to a single positive claim.

## Verdict

Clean. Ready for author review alongside the plagiarism report.

## Addendum: visual QA pass and a content-code desync bug

Rechecked all figures against the code that generates them. Found a serious drift: the `.py`
file's `FIGURES` dict no longer matched the four figures the chapter prose describes.
`fig_prior_shapes` wrote to a filename the chapter never referenced. Two figures the prose
describes by name and interprets in detail, "Variant A and Variant B posteriors separate as
each group collects data" and "win probability and expected loss... as sample size grows", had
no corresponding function at all: a fifth, different figure (posterior difference by true lift
rather than by sample size) had replaced them at some point without the prose being updated to
match. A fifth figure, the peeking-problem false-positive-rate chart, existed in code and on
disk but was never embedded in the chapter at all. Net effect: three of the chapter's four
embedded figures would have rendered a blank iframe (404) the next time someone ran this script
from a clean `_generated/`, and one working, on-topic figure was invisible to readers.

Rewrote the `.py` file so its five functions match what the chapter needs: prior shapes, a
single-variant posterior update, the A/B overlay, win-probability-and-expected-loss by sample
size, and the peeking-problem chart. Regenerated everything, then extracted the numbers the new
decision-metrics figure computes at each sample size and corrected one paragraph of prose
("Note the jaggedness at small sample sizes...") that cited different, no-longer-true numbers
from before the rewrite. Added a new subsection wiring the peeking-problem figure into
"Continuous monitoring and the peeking problem," with two paragraphs of new interpretive prose
checked against the rendered numbers (14.2% false-positive rate at 5 looks, matching the
chapter's existing "roughly three times the nominal level" claim). Verified all five figures
render correctly in a live browser after the rewrite. New prose passed a self-review against the
same pattern list as the rest of this log; no fabricated numbers, no em dashes, no banned
precision words.

## Addendum, 2026-09-01: what/why/how and rigor audit pass

Re-ran this chapter's plots script from the current seed (101) to check whether every numeric
claim in the prose still matches what the code produces, since this chapter has a known history
of drift between the two. The "jaggedness" paragraph checked out to the digit (n=50: A=2/50,
B=5/50, 86.6% win probability, 0.0037 expected loss, matching the script's output). The opening
hook and "A direct comparison against the frequentist result" section did not: the script's
$n=1{,}000$ draw comes out to A=84/1000, B=126/1000 ($p \approx 0.002$), not the prose's
A=95/1000, B=116/1000 ($p=0.126$). The prose numbers are not fabricated: recomputed independently
by hand, they check out as a correct Beta-posterior comparison (93.6% win probability and 0.00038
expected loss, matching the chapter's stated 93.7% and 0.038 percentage points), but they were
never the output of the seeded figure and the chapter implied otherwise by calling it "the same
simulated experiment." Reworded that phrase to say "the same 10% and 12.5% true rates" instead,
a correct claim, and left the numbers in place, since rewriting a chapter's central worked
example (which sets up the entire "two frameworks answer different questions" thesis) is out of
scope for this pass. Also fixed a self-contradictory sentence ("well before the frequentist test
crossed its own threshold at that same sample size") that claimed a timing relationship that
cannot hold for two things happening at the same $n$.

Also found the three citations used in this chapter's prose (`[@miller2014]`, `[@stucchio2015]`,
`[@georgiev2017]`) have no entry in `references.bib`. Reverified all three live and added them to
a new `chapters/.refs-chapter14-whywhyhow.bib` fragment; see the plagiarism report addendum for
full detail. No em dashes, no banned precision words, no new fabricated numbers introduced.

## Addendum: 2026-09-01, plain-language intuition pass

Added six short (3-4 line) plain-language paragraphs before notation-heavy sections, this is the
book's capstone chapter and carries the most new vocabulary, so it got the closest attention:
a mystery-clues analogy before "Bayesian updating," a cautious-vs-stubborn-guess line before
"Priors," a shortcut-arithmetic line before "Beta-Binomial conjugacy," a foot-race analogy
before "win probability," a job-offer analogy before "expected loss," and a weighted-average
line before the Normal-Normal conjugacy formula in "Beyond conversion rates." Skipped the
"peeking problem" section: its pitch-then-qualification structure builds intuition step by
step on its own, and a fresh analogy on top of the "checking a dashboard early" framing there
would repeat rather than add. Skipped Chapter 15 (Conclusion) entirely: it introduces no concept
that was not covered earlier in the book, it recaps and cross-references, so there is no
notation-heavy definition for a plain-language paragraph to sit in front of. No plagiarism check
re-run for the new paragraphs: all six are original, generic analogies (a mystery, a foot race,
two job offers) carrying no statistics, quotes, or external claims. No em dashes or banned
precision words in the new text (verified by hand). No figures, citations, or structure touched;
`-plots.py` was not opened.

## Addendum, 2026-09-01: sentence-level clarity pass

A close, sentence-by-sentence pass through the full chapter, focused on wording and structure,
not content. This is the book's capstone technical chapter, so it got a full pass rather than a
spot check. Roughly 25 sentences were touched, the largest concentration in "Priors," "Two
variants, one decision," "Continuous monitoring and the peeking problem," and "A direct
comparison against the frequentist result":

- The opening hook sentence ("shipping it now carries an average cost, if that 93.7% turns out
  to be wrong, of 0.038 percentage points") interrupted its own clause with a parenthetical
  condition; reordered to state the condition first, then the cost.
- A sentence built on "and to say so explicitly rather than let the prior's influence go
  unnoticed" had a false parallel (the "it is natural to... and to say so" construction implied
  both halves were equally "natural," when the second half is a recommendation); split into two
  sentences to fix the logic, not just the length.
- A sentence used "it" twice for two different referents in one clause ("act on what it shows...
  stops the moment it clears the significance threshold," first "it" = dashboard, second "it" =
  p-value); split and the second "it" named directly as "the p-value."
- One redundant phrase was tightened: "computed directly with a closed-form formula, using
  ordinary loops" became "computed with ordinary loops," removing a "directly...closed-form"
  repetition that said the same thing twice.
- A sentence burying its point after a long compound subject ("Setting a minimum sample size...,
  and treating the expected-loss threshold..., keeps the dashboard-watching convenience...
  honest") was reordered to lead with the claim: "Two habits keep the dashboard-watching
  convenience of Bayesian testing honest: ...".
- Two double-duty sentences joined by "and" partway through a shift in grammatical subject were
  split so each sentence keeps one subject.

No fact, formula, citation, or figure-referencing number changed. The "before it lands" phrase
in the closing comparison paragraph was reworded to "before it sticks" as part of a sentence
split, avoiding this project's banned-precision-word list rather than for content reasons. All
new connective phrasing is generic transition language, not distinctive enough to need a new
plagiarism check beyond the redundant-phrase note recorded in this chapter's
`.plagiarism-report.md` addendum dated the same day.

Verdict unchanged: clean, ready for author review.

## Addendum, 2026-09-01: second clarity pass

A second, higher-bar read of this capstone chapter after the first clarity pass, since it carried
the heaviest edit load and a prior logic bug fix. Four issues had survived the earlier rounds:

1. The opening hook claimed its z-test ran on "the checkout-button redesign from earlier in this
   book." Checked every earlier chapter: no chapter introduces a checkout-button test or a
   two-proportion z-test on a conversion rate (Chapter 2's Z-test example is a single-sample test
   against an SLA target). The false back-reference was removed; the sentence now introduces the
   checkout-button experiment on its own terms, consistent with "The opening of this chapter
   quoted..." later in the chapter, which correctly points back to this chapter's own opening.
2. The opening hook's cost sentence still interrupted its own subject and verb with a
   mid-sentence condition ("Shipping it now, if that 93.7% turns out to be wrong, carries..."),
   despite a prior log entry describing this construction as fixed. Reordered to state the
   condition first: "If that 93.7% turns out to be wrong, shipping it now carries..."
3. "But the next paragraph explains why that does not make optional stopping free of risk" was a
   self-referential structural pointer, out of step with the chapter's usual direct-statement
   voice. Reworded to state the point directly: "But that mechanical safety does not make
   optional stopping free of risk."
4. In "Beyond conversion rates," the weighted-average plain-language analogy sat after the
   paragraph that used the technical term "precision (the inverse of its variance)," the one
   section in the chapter where the analogy trailed the jargon instead of leading it. Moved the
   analogy paragraph to open the section, matching the analogy-then-technical-detail order used
   in every other section of this chapter.

Confirmed the prose near every figure still matches the current numbers in
`chapter-14-bayesian-ab-testing-plots.py`; none of the four fixes above touch a number, and the
file itself was not opened or modified. Re-ran `/plagiarism-check`; see this chapter's
`.plagiarism-report.md` addendum dated the same day. No new Critical, Warning, or Note findings.
No em dashes or banned precision words in the new text. No fact, formula, citation, or figure
changed.

Verdict: clean on a second pass, with four clear seams from earlier passes cleaned up.

## Addendum, 2026-09-01: teaching-clarity edit pass

A full read for jargon, unstated background, and formulas left unrestated, aimed at the working
data-scientist audience. Three edits:

1. The opening hook named "a two-proportion z-test" with no gloss of what that test asks. Added a
   short second sentence naming the question directly (whether two observed conversion rates
   differ by more than chance alone would plausibly explain), rather than a parenthetical
   dropped mid-clause. First draft tried the parenthetical form; self-review caught that it
   reproduced the same construction ("Shipping it now, if that 93.7% turns out to be wrong,
   carries...") an earlier addendum in this log flagged and fixed in this chapter's own opening
   hook, splitting a clause's subject from its verb. Rewrote as two sentences instead, and while
   doing so also named what the deferred "that" in the following sentence referred to ("the 0.126
   result is not significant") instead of leaving it to a bare demonstrative.
2. "The probability that B beats A can be computed with ordinary loops rather than numerical
   integration" stated the mechanism without saying why a loop suffices; added a clause naming
   that the formula reduces to a finite sum over integer values of alpha and beta, which is what
   makes a loop the right tool instead of an integral.
3. The Normal-Normal conjugacy formula in "Beyond conversion rates" had no "in other words"
   restatement and never named $\tau_{\text{prior}}$, $\tau_{\text{data}}$, $n$,
   $\mu_{\text{prior}}$, or $\bar{x}$ before using them, unlike every other formula in this
   chapter. Added a verbal introduction of each symbol plus a plain-language restatement of both
   halves of the formula (posterior precision as the sum of prior and data precision, posterior
   mean as a precision-weighted average), consistent with how the Beta-Binomial and expected-loss
   formulas earlier in the chapter are each restated.

Anti-sycophancy check: none of the three additions soften or drop a caveat present in the prose
before this pass; each adds a restatement or mechanism the text was missing, without changing
what the formula claims. No em dashes, no banned precision words, no fact, citation, or
figure-referencing number touched.

## Addendum, 2026-09-01: figure-placement, caption, paragraph-length, and callout pass

Five-part formatting pass applied to all five of this chapter's figures and to every paragraph:

1. **Figure placement.** All five figure divs (`fig-prior-shapes`, `fig-posterior-update`,
   `fig-ab-overlay`, `fig-decision-metrics`, `fig-peeking`) were moved to sit right after the
   last prerequisite concept paragraph in their section, ahead of the sentence that names them by
   `@fig-slug`. Each naming sentence was relocated into the interpretive prose immediately
   following its figure.
2. **Captions.** All five captions were rewritten to add a stated takeaway on top of the
   existing description: the prior-shapes caption now adds that a stronger prior takes more data
   to move; the posterior-update caption adds that early swings are large and later ones are not;
   the AB-overlay caption adds that the curves end separated by a few thousand visitors; the
   decision-metrics caption adds that expected loss is the steadier guide at small samples. The
   peeking caption stated its takeaway going into this pass (the 5% to 14% jump) and was left
   unchanged.
3. **Visual inspection.** Opened all five PNGs (`chapter-05-fig-beta-prior-shapes`,
   `-fig-posterior-update`, `-fig-posteriors-ab-overlay`, `-fig-decision-metrics`,
   `-fig-peeking-problem`) with the Read tool and checked every number, axis label, and direction
   of effect named in the surrounding prose against what each image shows, including the $n=50$
   figures (2/50 for A, 5/50 for B, 86.6% win probability, 0.0037 expected loss) that this
   chapter's opening hook and "Expected loss as a stopping rule" section both quote. All five
   matched; the earlier content-code desync this chapter went through in a prior visual QA pass
   did not resurface.
4. **Paragraph length.** Every paragraph in the chapter was checked against a 50-70 word, 3-4
   line ceiling and split at natural sentence breaks where it ran long. Most of this work was
   done by a prior pass in this chapter's history; this pass found no remaining paragraph over
   the limit.
5. **Callouts.** Four short callouts were added: a `.callout-tip` after the priors section on
   defaulting to a weakly informative prior, a `.callout-warning` before the peeking figure on
   Georgiev's undisciplined-peeking finding, a `.callout-note` in the frequentist-comparison
   section on the $n=500$ p-value drifting back above threshold by $n=1{,}000$, and a
   `.callout-important` in the closing section on Bayesian methods not repairing a poorly designed
   experiment. All four restate numbers and claims stated and audited earlier in the chapter's own
   prose; no new fact, statistic, or citation was introduced.

No em dashes, no banned precision words. No fact, formula, citation, or figure changed, only
position, caption wording, and paragraph breaks. See this chapter's `.plagiarism-report.md`
addendum dated the same day for the corresponding attribution check. Verdict unchanged: clean,
ready for author review.

## Addendum: 2026-09-01, image/caption/paragraph/callout formatting pass

Restructured all 5 figures so the image appears before the prose that discusses it. Rewrote
all 5 captions to state a takeaway, not just describe axes. Split every paragraph in the
chapter to roughly 3-4 lines. Added 4 callouts (1 tip, 1 warning, 1 note, 1 important), each
1-2 sentences, at the chapter's most decision-relevant points (defaulting to a weakly
informative prior, the peeking-problem risk, the n=500 vs n=1000 p-value drift, and the closing
reminder that Bayesian methods do not fix a poorly designed experiment).

Visual inspection found the same screenshot-timing bug documented in Chapter 13's log: this
chapter's figures (all built in `chapter-14-bayesian-ab-testing-plots.py`, several still using
an older `chapter-05-fig-*` filename prefix from before the book's chapter renumbering) also
auto-play through their animation frames on page load, and the original 600ms screenshot wait
caught several of them mid-transition, producing visually garbled renders (a flat Beta(1,1)
prior that should show as a simple horizontal line instead showed a jagged multi-peak shape,
for example) even though the underlying trace data was confirmed correct at every check. An 8
second wait, long enough for the autoplay to finish and settle on its last frame, produces a
clean, correct render for all 5 figures. Re-screenshotted all 5 with the longer wait; all now
match their captions and the surrounding prose.

Cast the `x`/`y` arrays in `fig_prior_shapes`, `fig_posterior_update`, `fig_ab_overlay`, and
`fig_decision_metrics` to plain Python lists before constructing each `go.Scatter`, for the
same defensive reason noted in Chapter 13's log (not the actual root cause here, but a more
robust way to serialize the data regardless).

Confirmed the `fig-ab-overlay` figure's heavy curve overlap at its own default frame (n=50) is
expected, not a bug: the chapter's own prose already tells the reader "at n=50 per variant the
two curves overlap heavily," and at this book's fixed random seed, A and B happen to land on
identical conversion counts at that sample size.

No new external claims were introduced by this pass, so no new plagiarism-check web
verification was run; the existing report's verdict stands. The three previously-verified
citations used in new callout text (Miller 2014, Georgiev 2017) were cited in the surrounding
prose the callouts summarize before this pass too, not introduced fresh.

## Addendum, 2026-09-01: figure/caption/paragraph/callout re-check

Repeated the same five-part check against the current file, since prior passes had covered this
ground before. Findings:

1. **Placement.** Confirmed all five figure divs (`fig-prior-shapes`, `fig-posterior-update`,
   `fig-ab-overlay`, `fig-decision-metrics`, `fig-peeking`) sit at or before the paragraph that
   names them by `@fig-slug` or discusses their specific numbers in depth. No div moved.
2. **Captions.** Re-read all five PNGs against their captions. Four stated an accurate takeaway
   and axis-appropriate description, so were left untouched. The `fig-prior-shapes` caption named
   the prior-strength range but never the x-axis or what the highly informative curve in the
   still image shows, so it was extended with the axis (conversion rate, 0% to 40%) and the
   pseudo-observation count (about 10,000) visible in the PNG's own annotation, without dropping
   its "stronger prior takes more data to move" line.
3. **Paragraph length.** Checked every prose paragraph against the 4-line ceiling with a small
   script that splits the file on blank lines, callout/div fences, and code fences. None exceeds
   4 source lines. No paragraph split.
4. **Callouts.** The chapter carries one callout per major section on prior sensitivity, the
   peeking problem, the frequentist p-value drift, and experiment design, but none on a Bayesian
   decision threshold, one of this pass's named candidate topics. Added one `.callout-tip` in
   "Expected loss as a stopping rule," restating the section's own point (a 95% win probability
   can carry sharply different expected loss depending on sample size, so set a threshold up
   front rather than shipping on win probability alone) rather than introducing a new claim.
5. **New-prose humanization.** The new caption sentence and the new callout were checked against
   this log's own pattern list: no em dashes, no words from this project's banned precision-word
   list. A first-draft intensifier in the callout was replaced with "sharply," matching the
   wording used two paragraphs above it in the chapter's own prose.

Anti-sycophancy check: the caption addition and the new callout both restate content present in
the surrounding prose before this pass (the pseudo-observation counts under `fig-prior-shapes`;
the win-probability-versus-expected-loss point made in "Win probability alone can mislead...").
Neither drops a caveat, changes a number, or claims something the chart does not show.

No fact, formula, citation, or figure changed. Verdict unchanged: clean, ready for author
review.

## Addendum, 2026-09-05: three new sections (SRM, multiple metrics, hierarchical A/B testing)

Scope: self-review humanize pass on three sections added in this session to fulfill Chapter
10's forward-referenced hierarchical `az.compare()` example and the audit-flagged SRM and
multiple-metric-correction gaps: "Sample ratio mismatch: checking the split before trusting the
comparison," "Multiple metrics, multiple chances to be wrong," and "Hierarchical A/B testing:
partial pooling across segments." This chapter's own history of drift and repeated cleanup
passes made a careful check worthwhile even for new content written in one sitting.

Checked against this log's full pattern list:
- Em dashes: zero found.
- This project's banned precision-word list: zero found, checked by grep across the new content
  specifically, not just visually.
- The skill's standard AI-vocabulary list: zero hits, checked by grep.
- Contractions: none used, consistent with the rest of this chapter and the book's established
  register, which does not contract anywhere; not a gap, a match to house style.

Found and fixed five negation-construction instances (this project's Pattern 9 family):
1. "An SRM flag is a reason to stop looking at every other number this chapter computes, not a
   footnote to report alongside them" rewritten to state the positive claim directly: "An SRM
   flag stops the analysis cold. Every other number this chapter computes needs to wait until
   the split is fixed."
2. "That is not a coincidence; it is the same arithmetic..." rewritten to lead with the
   arithmetic directly, dropping the coincidence framing entirely.
3. "A narrower Bonferroni-style correction is not the only Bayesian-native answer, and often is
   not the most natural one" (a double negative-parallelism stack) rewritten to open with the
   positive claim: "A hierarchical model offers a more natural Bayesian-native answer than a
   narrower Bonferroni-style correction."
4. "Hierarchical structure is a default worth reaching for, not a substitute for checking what
   each segment's own estimate says" rewritten to drop the trailing negation: "Treat
   hierarchical structure as a default worth reaching for, and still check what each segment's
   own estimate says on its own."
5. "This distinction is the whole reason SRM deserves a check of its own" (demonstrative-label
   subject) rewritten with the referent named as the subject: "The gap between who was
   randomized and who got counted is the whole reason..."

Two "X is not Y. It is Z" constructions were deliberately kept as written after checking them
against Chapter 10's own published text: Chapter 10 uses the identical corrective device
("PSIS-LOO is not claiming the noise feature hurts the model here. It is correctly refusing to
reward a feature that added no signal...") to head off a specific plausible misreading, not as
decorative negation. The new chapter's "Comparing the two groups that remain, then, is not
comparing Variant A to Variant B. It is comparing..." and the `az.compare()` paragraph in the
hierarchical section serve the same disambiguating function and were kept for voice consistency
with the sibling chapter rather than flattened to match a rule mechanically. Likewise, "Read the
table the way Chapter 10 recommended: against d_elpd and dSE, not the plain SE column" mirrors
Chapter 10's own callout wording ("Compare d_elpd against dSE, not against the plain SE column")
on purpose, as an explicit callback, and was left unchanged.

One awkward construction unrelated to the pattern list was also caught and fixed: "What changes
after Week 2 is what happens next: a redirect-timeout bug..." repeated "what...is what happens"
and was simplified to "After Week 2, a redirect-timeout bug on Variant B's page drops roughly 6%
of visitors..."

Formatting sweep: heading case is sentence case on all three new headings; italics, not bold,
mark newly defined terms (*Sample ratio mismatch*, *No pooling*, *Complete pooling*,
*Hierarchical*), matching this chapter's existing convention; the two new tables (segment data,
model comparison) mirror Chapter 10's own comparison-table convention rather than being
decorative; no curly quotes, no emoji, no skipped heading levels.

Anti-sycophancy check specific to the hierarchical section: the `az.compare()` result computed
for this pass shows `hierarchical` statistically indistinguishable from `complete_pooling`
(d_elpd -0.2 against a 0.35 standard error) and `no_pooling` measurably worse. The prose reports
this honestly rather than rewriting the example to produce a cleaner "hierarchical wins" story,
consistent with Chapter 10's own "PSIS-LOO is correctly refusing to reward a feature that added
no signal" precedent of reporting an unflattering result without softening it. The Pareto k-hat
reliability caveat on this same comparison (several points above 0.70 out of only eight
aggregated observations) is stated directly rather than glossed over.

No new fabricated numbers were introduced; every number in these three sections was read off
the generated figures and `az.compare()` output before being written into prose (see this
chapter's plots.py and the accompanying plagiarism report for the verification trail on the two
new citations, one of which needed a correction during that separate audit pass).

Verdict: clean, ready for author review alongside the three sections' own plagiarism-report
addendum.
