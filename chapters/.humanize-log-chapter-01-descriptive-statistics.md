# Humanize log: chapter-01-descriptive-statistics.md

Mode: BOOK (auto-inferred from `books-all/` path)
Run type: condensed pass (zero-tolerance + voice), not the full 4-pass pipeline. This is a
first-draft calibration chapter awaiting author review, matching the skill's own guidance to
reserve the full pipeline for pre-submission runs.

## What was checked

- Em dashes: zero found (structurally guaranteed: the project's write-time hook blocks every
  em dash and every banned precision word before a file can save).
- The skill's standard AI-vocabulary list (see `humanize-ai-text` Pattern 7): zero hits.
- Staccato stacks, demonstrative labeling, gerund headings, paragraph-terminal summaries,
  rule-of-three padding, vague "This" subjects: none found.
- Negative-parallelism density ("X, not Y" / "rather than" / bare negation): 9 instances found
  by an independent critique pass. 7 rewritten to direct positive phrasing. 2 left as-is
  (line ~108, ~256) because they describe a methodological choice between two named
  alternatives rather than a rhetorical contrast tic.
- Voice: independent critique confirmed one coherent, confident textbook-authorial voice
  throughout, with sentence-length variety that holds up.

## Fixes applied

1. "which is why it, not variance, is the number that shows up on dashboards" -> restated
   positively around "standard deviation, easier to read at a glance than a squared quantity."
2. "a high r says two variables move together, not that one drives the other" -> "it says
   nothing about which one, if either, drives the other."
3. "'Error' is worse than 'warn,' but not by a number you can add or subtract" -> "though the
   gap between them carries no numeric value."
4. "filling those gaps in with a plausible value rather than discarding the incomplete rows
   outright" -> "filling those gaps with a plausible value so the row can still be used."
5. "This distinction is not academic in a production system" -> "This distinction carries
   weight in a production system."
6. "Note that this does not make variance and standard deviation useless... a condition worth
   checking, not assuming" -> "Variance and standard deviation remain the right tool once a
   distribution is confirmed close to normal, worth a quick check before trusting the number."
7. "Note that this is not a quirk specific to kidney stones" -> "The same mechanism applies
   well beyond kidney stones."
8. Heading "Filling in missing values: mean imputation" (gerund opener, Pattern B1) -> "Missing
   values and mean imputation."

## Not applied

- Contraction insertion (Pattern 30): deliberately skipped. This chapter follows the
  book-writing-style-islr voice, which is a formal textbook register (matching ISLR itself,
  which does not contract). Forcing contractions in would work against the chosen voice.
- Full 4-pass worker architecture, parallel critique pairs, URL verification subagent: skipped
  in favor of a direct self-review plus one independent critique subagent, proportionate to a
  first-draft chapter still awaiting author sign-off on voice and structure.

## Verdict

Clean. No outstanding AI-pattern issues. Ready for author review alongside the plagiarism
report.

## Addendum: visual QA and storytelling-gap pass

Re-checked all 6 existing figures against the code that generates them and against a live
browser render of each `_generated/*.html` file. Found and fixed two layout bugs in
`chapter-01-descriptive-statistics-plots.py`: the mean/median annotation labels in
`fig_mean_vs_median` overlapped the chart title, and the p90/p99 labels in
`fig_std_vs_percentile` could clip above the plot area; both now sit inside the plot as
corner-anchored annotations, matching the pattern used elsewhere in this file. Cleaned up 5
orphaned figure files in `_generated/` left over from an earlier draft of this chapter, never
referenced by the current `.md`.

Added one new figure, `chapter-01-fig-literary-digest`, a predicted-vs-outcome toggle chart for
the Literary Digest selection-bias story: the section had the numbers (57% predicted, 60.8%
election result) but no chart showing the gap. Fixed one negative-parallelism construction
("the gap is not noise, it is...") in the new caption on self-review.

## Addendum: 2026-09-01, what/why/how narrative-completeness and claim-rigor audit

Audited the full chapter against two criteria: whether every major concept hits a plain WHAT
before its formula, a concrete WHY (a concrete consequence, not an assertion), and a worked HOW;
and whether any passage claims more confidence than it has earned or omits a tradeoff worth
naming. Section by section, the chapter clears both bars on its own: mean/median/mode,
variance/standard deviation, covariance/correlation, skewness/kurtosis, population/sample, data
types, selection bias, missing values and imputation, and Simpson's paradox all carry their own
worked example and an honest limitation where one exists (the std.-normality caveat, the MCAR
caveat on mean imputation). No narrative gaps found requiring a fix.

One rigor gap found and fixed: the "measures of spread" section recommends percentile-based
alert thresholds over mean-plus-k-std thresholds, citing the Google SRE book, but stated the
percentile recommendation without its own tradeoff, while the std.-based approach two sentences
earlier got an honest caveat. Added one paragraph noting that p99 and higher percentiles need
enough requests per window to be stable, and a low-traffic endpoint or narrow window can produce
a noisy, meaningless p99. Confirmed via WebSearch as standard, widely documented production
engineering knowledge (IBM, Aerospike, Redis, and OneUptime write-ups on p99 stability at low
sample sizes), written in fresh phrasing, no citation required. Re-ran `/plagiarism-check` on the
full chapter afterward; the new paragraph classified CLEAR, verdict unchanged at
PUBLICATION-READY. No em dashes or banned precision words introduced. No headings, figures, or
structure touched.

## Addendum: 2026-09-01, plain-language intuition layer

Added nine short plain-language analogy paragraphs (40-60 words each), one before the notation
in every major section, so a reader without calculus or summation-notation background can
follow the idea even if not every formula. Kept to the author's explicit length constraint:
3-4 lines, roughly 40-60 words, trimmed down on the first draft to fit. Zero em dashes, zero
banned precision words; the write-time hook caught and forced a rephrase of a handful of stray
instances across drafting. Re-ran `/plagiarism-check`; all nine new paragraphs classified CLEAR.
No existing prose, formulas, figures, or headings were removed or altered.

## Addendum: 2026-09-01, sentence-level clarity pass

Fourth pass: a close, sentence-by-sentence clarity edit of the existing prose, not a content
add. Roughly 24 sentences touched across 12 locations. Most edits split a long sentence (35+
words, often stacking a colon, a semicolon, and a trailing "which is why" clause) into two
shorter ones, so each sentence carries one idea instead of two or three. Two edits fixed a
clarity problem rather than just length: the missing-values paragraph's example list ("an
instrumentation library that fails to emit a trace, a dropped log line, a field added to the
schema...") had a dangling "so" clause that read as attached to the whole list instead of the
last item, fixed with "or" and "so that"; and the mean-imputation closing sentence buried a
three-clause nested parenthetical inside one long "when" clause, restructured into three shorter
sentences that state the timeout mechanism before naming its consequence. No word-count
reduction was a goal or a side effect worth noting either way; a handful of sentences grew
slightly longer where splitting required restating a subject. Re-ran `/plagiarism-check`; the
report's addendum confirms no new matches for the rewritten phrasing. Zero em dashes, zero
banned precision words introduced (write-time hook caught one word during drafting, rephrased
before save). No formulas, citations, figures, or headings touched.

## Addendum: 2026-09-01, second clarity pass

Fifth pass, a higher-bar re-read on top of the sentence-level pass above: most of the chapter
read clean on this second look, so the touch count is small. Found and fixed two issues, both in
"Measures of spread: variance and standard deviation." First, the section used three different
shorthand spellings of "standard deviation" within a few sentences of each other
("mean-plus-$k$-std," "std-based," "std.-deviation"), while the rest of the chapter always
spells the term out; normalized all three to the spelled-out form. Second, the sentence "Variance
and standard deviation remain the right tool once a distribution is confirmed close to normal,
worth a quick check before trusting the number" left "worth a quick check" dangling with no clear
subject, forcing a re-read to work out that the check refers to confirming normality; rewrote as
"...a distribution is confirmed close to normal, a check worth running before trusting the
number." No other sentence in the chapter needed a fix on this pass: no jargon-before-analogy
mismatches, no topic-sentence mismatches, and no other inconsistent terminology found. Re-ran
`/plagiarism-check`; both fixes classified CLEAR, no new source match, verdict unchanged at
PUBLICATION-READY. Zero em dashes, zero banned precision words. No facts, numbers, formulas,
citations, figures, or headings touched.

## Addendum: 2026-09-01, clarity and jargon-gap edit pass

Sixth pass: a word-by-word clarity read targeting unstated jargon, formulas left without a
plain-language restatement, and terms used before they were defined. Four fixes applied, all
additive (nothing removed, no structure changed):

1. Variance section: the variance formula had a plain-language description before it but no
   restatement after, breaking the pattern every other formula in the chapter follows. Added a
   sentence naming what $\sigma^2$ denotes, restating the formula in plain words ("the average
   squared distance between a request and the typical request"), and a practitioner note that
   NumPy's `np.var` (divides by $n$) and pandas' `.var()` (divides by $n - 1$) disagree by
   default, a trap worth knowing about before porting this formula into code.
2. Measures of spread section: "percentile" was used (p90, p99) without a definition anywhere in
   the chapter body. Added a parenthetical definition at first substantive use.
3. Same section: "This is because standard deviation assumes a shape the data does not have"
   overstated the claim, since standard deviation itself makes no shape assumption; the
   assumption belongs to the mean-plus-k-std threshold rule. Reworded to attribute the
   assumption to the threshold, not the statistic.
4. Missing values section: "multiple imputation" was named as the more careful alternative to
   mean imputation but never described. Added a short clause explaining what it does differently
   (draws a range of plausible values instead of one fixed number).

Applied the humanization patterns documented above to this new prose: no em dashes, no banned
precision words (the write-time hook enforced this during drafting), varied sentence rhythm,
symbol-before-use notation per the book-writing-style-islr guide, and no motivational filler. Ran
an anti-sycophancy self-check on all four edits: the NumPy/pandas ddof claim was checked against
each library's documented default instead of assumed; the standard-deviation fix corrects an
imprecision instead of introducing one; the percentile and multiple-imputation additions are
standard, uncontested definitions that add rigor without softening or dropping any existing
caveat. No regressions found. Re-ran `/plagiarism-check` on the four new passages; see the
plagiarism report's matching addendum. No facts, formulas, figures, citations, or headings were
removed; only clarifying language was added.

## Addendum: 2026-09-01, image/caption/paragraph/callout formatting pass

Five changes applied throughout the chapter:
1. Every figure moved to appear before the prose that discusses it (right after the minimal
   setup needed to make the chart legible), instead of after a lead-in paragraph.
2. All seven captions rewritten to state the takeaway (e.g. "even a 5% slow-request share
   more than doubles the gap between mean and median") rather than only describing the axes.
3. Visual inspection via the Read tool against each figure's current PNG caught two real
   rendering bugs, both fixed in `chapter-01-descriptive-statistics-plots.py` and regenerated:
   the Literary Digest figure's x-axis labels stayed stuck on "(predicted)" after switching to
   the "Election result" frame (Plotly does not reliably redraw categorical tick text across
   frames with different category sets); the Simpson's paradox figure showed 4 stale x-axis
   category slots when switching to the 2-bar "Combined" view. Both rebuilt with a fixed
   numeric x-axis and layout-level tick labels so the bug class cannot recur. Also found and
   fixed a systemic issue where several figures' default (page-load) slider frame did not
   match frame 0 as intended (a Plotly HTML-export quirk); all seven of this chapter's PNGs
   were regenerated forcing the correct default frame.
4. Every paragraph split to a 3-4 line maximum; no paragraph in the chapter now runs longer
   without a break.
5. Eight `.callout-*` boxes added (tip/note/important/warning), each 1-2 sentences,
   highlighting a specific gotcha, rule of thumb, or caveat already present in the prose
   rather than newly asserted.

No new external claims, citations, or statistics were introduced; every number in the chapter
was verified in prior passes. No `/plagiarism-check` web sweep was re-run since nothing new
needs verification, this addendum documents that reasoning directly. Verdict unchanged:
PUBLICATION-READY (local review only).

## Addendum: 2026-09-01, second placement/caption/paragraph/callout pass

Re-checked all seven figures against their current PNG exports and against the surrounding
prose. Placement was correct for every figure going into this pass (each div sits at or
immediately before the paragraph that first discusses it in depth) and all seven captions
named the axes and the pattern the chart makes visible, so neither figure placement nor
caption text changed this pass.

Four paragraphs that had grown back past a 4-line source limit were split at their natural
sense break, with no sentence fragmented and no clause left orphaned: the opening
"Descriptive statistics is the set of tools..." paragraph, the "To keep the chapter
grounded..." paragraph, the "This is because skewness and kurtosis are not two independent
knobs..." paragraph, and the "Suppose an engineering team wants to know the typical
latency..." paragraph in Selection bias.

Three new callout boxes were added, one each in the three major sections that had none going
in (Skewness and kurtosis, Population and sample, Data types), keeping every other section at
its existing callout count: a skewness-magnitude rule of thumb (`.callout-tip`), the
Greek-letter-for-population vs. Latin-letter-for-sample notation convention
(`.callout-note`), and the common mistake of averaging an ordinal field like log severity
(`.callout-important`). None duplicates an existing callout's point.

Applied this chapter's established humanization patterns to all new prose: zero em dashes,
zero banned precision words (write-time hook enforced), no gerund-opener headings, no
paragraph-terminal summaries, and sentence rhythm consistent with the surrounding voice. Ran
an anti-sycophancy self-check on every edit: the paragraph splits change no wording, only
placement of the break; the skewness rule of thumb states the standard Bulmer-style threshold
(|skew| < 0.5 roughly symmetric, > 1 highly skewed) without overclaiming precision; the
population/sample notation callout states a standard convention; the ordinal-averaging
callout illustrates a mistake using the chapter's own severity-level example rather than a
new claim. No regressions found. Verdict unchanged: PUBLICATION-READY (local review only).

## Addendum: 2026-09-05, gedeck-enhancement pass (robust statistics, Spearman correlation, new figure)

Mode: BOOK. Run type: condensed pass (zero-tolerance plus voice self-review against the full
humanize-ai-text pattern list), matching this chapter's established practice of reserving the
full 4-pass pipeline for pre-submission runs and applying a direct self-review to first-draft
additions still under author review.

Scope: only the two newly added sections, "Robust statistics: trimmed mean, IQR, and MAD"
(inserted after Measures of spread) and "Spearman correlation and correlation across many
metrics" (inserted after Covariance and correlation, including one new figure,
`fig-metric-correlations`). No existing prose, figures, or headings were reviewed or touched
in this pass.

### What was checked

Read both new sections against the full pattern list (removal patterns 1-53 plus the
book-specific B1-B7 patterns), matching the standard this chapter has held across every prior
addendum.

### Findings and fixes applied

1. **Bolded section-label pattern (9b, bolded form)**: three mini-headers, `**Trimmed mean.**`,
   `**Interquartile range.**`, and `**Median absolute deviation.**`, opened paragraphs inside
   the robust-statistics section as bold-plus-period section organizers. This chapter has never
   used bolded inline labels anywhere else, relying instead on an italicized `*term*` on first
   use. Removed all three; each paragraph now opens directly with the italicized term, matching
   the chapter's own established convention.
2. **Negative parallelism (Pattern 9, multiple forms)**: five instances rewritten to direct
   positive phrasing. "The modified z-score threshold of 3.5 is a widely used rule of thumb,
   not a law" became a sentence stating why Iglewicz and Hoaglin chose 3.5 (matched
   false-positive rate against the classical z-score rule), which also adds a fact rather than
   only removing a hedge. "The relationship is not a moderate one" became "The relationship is
   strong," continuing directly into the supporting sentence. "Some relationships are best
   described that way: not by how much one variable moves... but by whether..." was rewritten
   as a direct question ("do the two quantities always move in the same direction, whatever
   the size of each step?"). "This is the same confounding-variable pattern from the
   correlation section above" (a demonstrative-labeling instance, Pattern 9b) was rewritten to
   name the pattern's origin as the subject directly.
3. **Imperative reader-address outside the established slider convention**: "Before writing
   down a formula, look at what happens..." and "Return to the alert-threshold problem from the
   previous section" used a directive voice this chapter reserves specifically for slider
   interactions ("Drag the slider to 5%..."). Rewritten to declarative statements matching the
   chapter's non-figure prose register elsewhere.
4. **AI vocabulary, em dashes, hedge stacks, filler phrases, rule-of-three overuse, passive
   voice clusters**: zero instances found in either new section on this pass.
5. **Contractions**: none inserted, consistent with this chapter's established formal register
   (see the "Not applied" note on contraction insertion in the original run above, which this
   addendum's new prose also follows).

### Not applied

- Full 4-pass worker architecture, parallel critique subagents, URL verification subagent:
  skipped for the same proportionality reason as every prior addendum on this chapter, a
  focused addition still under author review rather than a pre-submission pass. The formula
  for the modified z-score threshold's constant (0.6745 divided into raw MAD, equivalently
  1.4826 times raw MAD) and Tukey's 1.5x IQR fence were independently verified via WebSearch
  during the accompanying plagiarism-check pass, not this humanize pass; see this chapter's
  `.plagiarism-report.md` addendum dated 2026-09-05 for those citations.

### Verdict

Clean after the five fixes above. No outstanding AI-pattern issues in the new content. Ready
for author review alongside the updated plagiarism report.
