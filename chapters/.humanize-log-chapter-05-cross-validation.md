# Humanize log: chapter-cv-cross-validation.md

Mode: BOOK (auto-inferred from `books-all/` path)
Run type: condensed pass (zero-tolerance + voice), matching the process used for chapter 1.

## What was checked

- Em dashes and the skill's standard AI-vocabulary list: zero found (structurally guaranteed by
  the project's write-time hook).
- Staccato stacks, demonstrative labeling, gerund headings, paragraph-terminal summaries, rule-
  of-three padding, vague "This" subjects: none found.
- Negative-parallelism density: one "not because X, but because Y" trailing-contrast instance
  found and rewritten to a direct positive statement. Two remaining negations ("rather than a
  theorem", "not the only way... it is") were left as-is: both are technical distinctions or
  definitional pivots, not rhetorical contrast tics.
- Voice: one coherent, confident textbook-authorial voice throughout, consistent with chapters
  1 and 4's established register.

## Fix applied

1. "the standard choice, not because either number is special, but because both sit in the
   range..." -> "the standard choice because both sit in the range..."

## Not applied

- Contraction insertion: skipped, matching the book-writing-style-islr formal register this
  book has used from chapter 1 onward.

## Verdict

Clean. No outstanding AI-pattern issues.

## Addendum: visual QA pass (2026-09-01)

Read all 4 figure functions in `chapter-05-cross-validation-plots.py` and confirmed the
statistics computed (train/test MSE by polynomial degree, validation-strategy variance across
60 repeats, CV-selected lambda, ELPD approximation) match what the chapter prose claims. Ran the
script to confirm clean regeneration, then visually verified all 4 figures in a browser: the
U-shaped test-error curve, the box-plot spread narrowing from single-split to LOOCV, the
U-shaped lambda-selection curve, and the ELPD complexity curve all render correctly and match
their captions. No content gap found in this chapter worth a new figure; every major concept has
a supporting chart. No changes made.

## Addendum: what/why/how and rigor pass (2026-09-01)

One rigor fix, no narrative gaps found. The PSIS-LOO paragraph stated the method "stays reliable
even when a handful of observations wield an unusually large influence on the fit," which
overstated the method's guarantees given its own reliability threshold. Rewrote the sentence to
state that the approximation is not reliable by default in that case, and that the method's own
Pareto-k diagnostic is what flags it, checked against the `loo` package documentation and the
Vehtari, Gelman, and Gabry (2017) paper cited in this chapter. Adjusted the neighboring
WAIC-comparison sentence so the diagnostic explanation is not stated twice. Every other section
in this chapter was reviewed against the what/why/how checklist and needed no change:
training-error motivation, the validation-set/LOOCV/k-fold progression, the bias-variance case
for k=5 or k=10, the lambda-selection worked example, and the data-leakage and time-series
limitations section all state a plain definition, a concrete consequence, and a worked example
or decision rule.

New prose checked for em dashes and the project's banned-vocabulary list: none found. No
structural AI patterns introduced.

## Addendum: 2026-09-01, plain-language layer pass

Added two short (3-4 line) plain-language intuition paragraphs: a studying-for-a-test analogy
before the training-vs-test-error section, and a retaking-a-modified-test analogy before
PSIS-LOO. Most of this chapter carries light notation with prose-based explanations throughout,
so these two spots (the core motivating concept and the one section with the most unexplained
jargon) were the places that needed it; the rest was judged accessible without addition.
Checked for em dashes and banned vocabulary: none found.

## Addendum: 2026-09-01, sentence-level clarity pass

Fourth pass: a close, sentence-by-sentence clarity edit of the prose (not a content pass; every
formula, citation, figure, and fact is unchanged). Roughly 17 sentences touched, spread across
the whole chapter with a concentration in "Choosing k," "What cross-validation does not fix," and
the Bayesian perspective section. Kinds of edits made:

- **Split overlong sentences** carrying two or three chained clauses (a semicolon plus "and," or
  a "because" clause tacked onto a sentence that had a complete thought of its own) into shorter,
  single-claim sentences. Example: the LOOCV validation-set-noise sentence, previously one long
  semicolon chain ending in "a different choice of model," now reads as three shorter sentences.
- **Fixed a comma splice.** "Consecutive requests during a traffic spike are not independent
  draws from a fixed distribution, they are correlated in time" became two sentences.
- **Removed a redundant self-referential construction.** "The gap between the two curves is the
  gap a data scientist cannot see" became "That gap between the two curves is invisible to
  anyone who only tracks training error," dropping the repeated word and stating the point once.
- **Named an ambiguous pronoun's referent.** "None of this replaces classical k-fold
  cross-validation" became "None of this Bayesian machinery replaces classical k-fold
  cross-validation," so the reader does not have to scan back several sentences to resolve "this."
- **Removed one filler opener** ("Note that this is an empirically supported default...").

Checked for em dashes and the project's banned-vocabulary list: none found. No structural AI
patterns introduced. Re-ran `/plagiarism-check`; see the addendum in the chapter's
`.plagiarism-report.md` dated the same day. Verdict: clean, no outstanding issues.

## Addendum: 2026-09-01, second clarity pass

A second, higher-bar clarity pass. This chapter read cleanly overall; three spots got touched:

- **Terminology inconsistency, cross-chapter.** The opening paragraph called the regularization
  example's noise variable a "noise feature," while Chapter 4, where that example lives, uses
  "noise predictor" everywhere else for the same variable. Fixed both chapters to "noise
  predictor" for consistency.
- **Imprecise comparison.** The PSIS-LOO paragraph called it "the Bayesian analog most directly
  comparable to k-fold CV." Since PSIS-LOO is a leave-one-out method and this chapter frames
  LOOCV as k-fold CV's k=n special case, "classical LOOCV" states the tighter, correct match and
  fits the rest of the paragraph, which compares PSIS-LOO's cost against LOOCV's refitting cost
  specifically.
- **Missing citation on a named method.** WAIC was introduced by name with no citation, though
  the citation sitting two sentences earlier in the same paragraph (Vehtari, Gelman, and Gabry
  2017) covers it directly, per that paper's own title. Added the citation to the WAIC sentence
  rather than leaving a named result unattributed next to a cited one.

No formula, fact, or number was changed. Checked for em dashes and the project's
banned-vocabulary list: none found. Re-ran `/plagiarism-check`; see the matching addendum in
`.plagiarism-report.md`. Verdict: clean, no outstanding issues.

## Addendum: 2026-09-01, third clarity pass (jargon and background-assumption sweep)

A close, word-by-word pass focused on unstated jargon, unexplained logical jumps, and assumed
background rather than sentence mechanics (those were covered by the two prior clarity passes).
Four edits, no content, formula, citation, or number changed:

- **Concretized a vague "shortcut" claim.** The LOOCV section said "certain linear models have a
  shortcut" with no indication of what the shortcut is or which models qualify. Named ordinary
  least squares as the concrete case and described the mechanism in plain language: a formula
  recovers each leave-one-out error from the single full-data fit rather than requiring $n$
  separate refits. No formula symbols were added, so no new notation for the reader to parse.
- **Added a "Recall" bridge into the Bayesian perspective section.** This chapter's Bayesian
  section leans on "posterior distribution" without pointing back to where that term was built
  from scratch (Chapter 4's own "A Bayesian perspective" section, which uses a prior-narrows-to-
  posterior analogy). Added an explicit Recall sentence, consistent with the book-writing-style-
  islr rule that every new section ties back to prior material.
- **Defined "posterior draws" on first use.** This chapter uses the phrase three times but never
  states what a draw is. Chapter 4 mentions Markov chain Monte Carlo sampling once, in passing,
  without unpacking it either, so a reader arriving at PSIS-LOO had no plain-language anchor for
  the term. Added a one-clause definition tied to that same MCMC mention: a draw is one sampled
  parameter value out of the many an MCMC fit produces in place of a single point estimate.
- **Named what "Pareto-k" is.** The Pareto-k diagnostic was referenced by name with no
  indication of what the letter k measures. Added "a Pareto shape-parameter estimate," which
  matches the Vehtari, Gelman, and Gabry (2017) paper cited in this chapter and the plagiarism
  report's own prior verification of that same fact (Pareto shape parameter k, roughly k < 0.7
  for reliability).

Anti-sycophancy self-check on these four edits: none of them soften or drop a caveat. The LOOCV
shortcut edit keeps the existing "does not extend to most of the models later chapters cover"
limitation intact. The MCMC-draw definition anchors to the mechanism the book named for
approximating a posterior (MCMC) rather than overclaiming that all posterior sampling works that
way. The Pareto-k description states only what the parameter is, not a stronger reliability claim
than the surrounding sentences make. No regressions found.

Checked for em dashes and the project's banned-vocabulary list: none found in the new sentences.
Re-ran `/plagiarism-check` reasoning on the new sentences; see the matching addendum in
`.plagiarism-report.md`. Verdict: clean, no outstanding issues.

## Addendum: 2026-09-01, figure-first restructure, caption, and callout pass

Fifth structural pass, covering the five parts of a book-wide editing task run across chapters
4 through 6.

1. **Figure placement.** All 4 figure divs moved so each appears immediately after its section
   heading, or after the minimal definitional setup a chart's axis labels require: the k-fold
   definition ahead of the validation-strategy-variance box plot, and the ELPD definition ahead
   of the ELPD-by-complexity chart. Every sentence that used to introduce a figure by name ahead
   of the image now follows the image instead.
2. **Captions.** All 4 captions rewritten to state the chart's takeaway. The lambda-selection
   caption was the thinnest in the original (described the U-shape without saying what it means
   for the reader's decision); it now names the minimum as the value cross-validation would pick.
3. **Visual inspection.** Opened all 4 PNGs with the Read tool and checked each against its
   surrounding prose. The train/test-error and ELPD-by-complexity charts both carry an in-chart
   annotation marking degree 2 as the turning point (lowest test error, highest ELPD), which
   matches the prose's "both mark the same transition" claim closely. The validation-strategy
   box plot's spread narrows from single split to 5-fold to 10-fold to LOOCV, matching the prose.
   The lambda-selection curve's U-shape and marked minimum match the prose. No mismatch found.
4. **Paragraph length.** Roughly a dozen paragraphs over 4 lines split at natural sentence
   breaks, concentrated in the validation-set-noise paragraph, the LOOCV/k-fold definitions, the
   bias-variance trade-off section (previously one 140-word paragraph, now three), and the
   data-leakage section.
5. **Callouts.** Three added: a `.callout-note` on training error only ever falling or holding
   flat as flexibility rises, a `.callout-tip` on the OLS closed-form leave-one-out shortcut, and
   a `.callout-warning` on preprocessing-driven data leakage.

No formula, citation, fact, or number was changed. Zero em dashes, zero banned precision words
(verified by grep across the full file after editing). Re-ran `/plagiarism-check`; see the
matching addendum in `.plagiarism-report.md` dated the same day. Verdict: clean, no outstanding
issues.

## Addendum: 2026-09-01, sixth pass (paragraph length, callout, and placement re-check)

Re-verified figure placement across all 4 divs: each sits at or before the paragraph that
discusses it in depth, immediately after its section heading and ahead of the discussion, so no
figure moved this pass. Re-read all 4 PNGs against their captions: all four state the axes and
pattern with accuracy, so no caption changed.

Split two paragraphs that had grown to 5 source lines at their existing sentence boundary: the
polynomial-degree/test-error setup paragraph (now two paragraphs) and the WAIC-vs-PSIS-LOO
comparison paragraph (now two paragraphs). No sentence was reworded, only the paragraph break
moved.

Added three new callouts, one per section, none duplicating an existing callout's point: a
`.callout-tip` on preferring k-fold over a single split below a few hundred observations
(validation-set-approach section), a `.callout-tip` restating the k=5/k=10 default as a
quick-reference rule (choosing-k section), and a `.callout-important` on the time-series
shuffling pitfall (what-cross-validation-does-not-fix section, alongside the existing leakage
warning).

Anti-sycophancy self-check: the two paragraph splits changed no wording, only line breaks, so no
claim or caveat was altered. All three callouts restate a claim stated in the immediately
preceding prose, checked against that prose before being written; none introduces a new fact.

Checked for em dashes and the project's banned-vocabulary list across the full file: none found.

Verdict: Clean. No outstanding AI-pattern issues.

## Addendum: from-scratch k-fold grid-search section, humanize pass (2026-09-05)

Condensed pass (zero-tolerance + structural + voice, matching the process used for earlier
condensed additions to this chapter), scoped only to the new section "Building k-fold
cross-validation from scratch: tuning two knobs at once," inserted between "Cross-validation for
model selection: picking lambda" and "What cross-validation does not fix." Nothing else in the
chapter was touched by this pass.

### What was checked and fixed

- **Em dashes**: zero found (structurally guaranteed by the project's write-time hook).
- **AI vocabulary list**: zero found.
- **Negative parallelism (Pattern 9, multiple forms)**: four instances found and rewritten to
  state the positive claim directly instead of defining by exclusion:
  1. "That model carries two knobs, not one" -> "That model carries two knobs."
  2. "This is what a grid search is: cross-validating every candidate combination rather than
     one value at a time, so a single winning cell can be read off the table instead of guessed
     at." -> "A grid search cross-validates every candidate combination this way and reads the
     single winning cell off the resulting table." (also fixes a Pattern 9b demonstrative label,
     "This is what X is").
  3. "comes from a mobile app team's crash telemetry rather than the latency model this chapter
     has used so far" -> "comes from a mobile app team's crash telemetry, a new dataset for this
     section, separate from the latency model used earlier in this chapter."
  4. "GridSearchCV builds its own folds internally rather than reusing the fold_id array above,
     so its numbers will not match the table cell for cell, but it is searching the identical
     grid..." -> "GridSearchCV builds its own folds internally, so its numbers will not match the
     table cell for cell, even though it searches the identical grid..."
  5. "just written as an explicit loop instead of a one-line split" -> "written here as an
     explicit loop over every observation."
  One instance judged acceptable and left as-is: "called once per grid cell instead of once
  overall" states a repetition-count fact (how many times the k-fold procedure runs), not a
  definitional exclusion, so it does not fit the pattern this rule targets.
- **Synonym cycling (Pattern 12)**: the concept of tuning one hyperparameter before the other was
  named five different ways across two paragraphs and a callout ("a search that tunes one knob
  before touching the other," "the one-knob-at-a-time search," "the sequential search," "a search
  that tunes one at a time," "a search that fixes one before touching the other"). Standardized
  on "a one-at-a-time search" at first mention, then "the one-at-a-time search" for every
  subsequent reference, matching the book-mode running-example continuity rule.
- **Staccato stacks, rule of three, filler phrases, hedging, authority tropes, generic
  conclusions**: none found.
- **Subheading style**: the new heading, "Building k-fold cross-validation from scratch: tuning
  two knobs at once," opens with a gerund. Reviewed against this chapter's own established mixed
  heading style (for example, "Choosing k: the bias-variance trade-off," present and unchanged in
  this chapter, uses the identical gerund-plus-colon-plus-specific-claim form). Judged consistent
  with existing precedent rather than a violation; left unchanged.
- **Contractions**: none added, matching this chapter's formal register (see the original
  humanize pass note above: contraction insertion skipped throughout this chapter).
- **Grounding**: every claim in the new section's "why it matters" argument carries a computed
  number (10.57, 10.62, 10.05, roughly 5 percent) rather than an abstract assertion, satisfying
  the addition-pattern requirement to ground claims concretely.

### Verdict

Clean after the fixes above. No outstanding AI-pattern issues in the new section. Re-ran the
project's em-dash and banned-vocabulary grep across the full chapter file after editing: zero
hits. Chapter-wide verdict remains: PUBLICATION-READY (local review only, per the standing
"do not publish" instruction on this project).
