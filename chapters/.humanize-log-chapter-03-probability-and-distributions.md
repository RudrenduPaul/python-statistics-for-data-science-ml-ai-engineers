# Humanize log: chapter-03-probability-and-distributions.md

Mode: BOOK (auto-inferred from `books-all/` path)
Run type: condensed pass (zero-tolerance + voice self-review), consistent with chapters 1-2.

## What was checked

- Em dashes and the project's banned precision-word list: caught and fixed during drafting via
  a scratch-file grep pass mirroring the write-time hook's own whole-word matching, before the
  final Write call. Final saved version is clean.
- Negative-parallelism density: reviewed. The few "not X" constructions carry substantive
  content ("checkout-API latency is right-skewed, not normal, because..." states a fact
  Chapter 1 established with a reason attached, not a rhetorical tic) rather than padding.
- Gerund headings, paragraph-terminal summaries, staccato stacks, demonstrative labeling: none
  found on review.
- Running example continuity: the anomaly-alert scenario (new to this chapter, feeding forward
  into Part 2's Bayesian A/B testing) and the checkout-API narrative from Chapters 1-2 both
  carry through consistently; every distribution is tied to a production mechanism (retries,
  request arrivals, shard assignment, canary selection) rather than a generic dice or card
  example.
- Citation accuracy: the SHAttered SHA-1 citation was checked against CWI's own primary
  announcement page; see the chapter's plagiarism report.

## Verdict

Clean. No outstanding AI-pattern issues.

## Addendum: visual QA and storytelling-gap pass

Re-checked all 6 existing figures against the code that generates them and against a live
browser render of each `_generated/*.html` file: Bayes base-rate, normal/empirical rule,
Poisson shape, geometric retries, CLT simulation, and birthday/hash collision all render
cleanly with correct titles, axes, and slider ranges. No bugs found. Found and removed 5
orphaned figure files in `_generated/` (bayes-posterior, canary-pod-sampling, clt-convergence,
hash-collision, poisson-arrivals) left over from an earlier iteration of this chapter's plots
script; none were referenced by the current `.md`, and the citations tied to earlier drafts of
those figures (SHA-1) are still cited correctly against the figure that replaced them.

Storytelling gap: the binomial distribution section stated the formula and named two production
examples (failed requests in a batch, A/B bucket conversions) but had no figure, unlike every
other distribution in the chapter. Added `chapter-03-fig-binomial-conversions`, the PMF of
conversions out of a fixed 50-visitor bucket with a slider on the true conversion probability,
directly setting up the Beta-Binomial framing Part 3 builds on. Embedded it in the binomial
section.

## Addendum: 2026-09-01, what/why/how and rigor audit

Audited the full chapter against two criteria: whether every major concept states what it is,
why it matters (a concrete consequence, not an assertion), and how it is computed, and whether
any claim carries more confidence than it has earned.

Narrative gap fixed: the canary-cohort section (Selecting without replacement) set up a
hypergeometric probability calculation (5 accounts drawn from a pool of 200, 3 known heavy
users) but never solved it, unlike the Bayes and geometric sections, which both carry their
worked examples through to a number. Added the completed calculation, about 92.6% chance no
heavy account lands in the canary by chance, so roughly a 7% chance one does. This is the how
beat the section was missing.

Rigor issue fixed: the Central Limit Theorem section stated that a t-test can tolerate
non-normal data "once the sample is large enough" and that the CLT "delivers that for free at
scale," without qualifying how large is large enough. This overstated the universality of the
rescue: convergence speed depends heavily on how skewed or heavy-tailed the underlying
distribution is, and some latency-style long-tailed distributions need a sample far past the
usual rule-of-thumb size before the normal approximation holds. Added a caveat sentence stating
this dependency directly, with the checkout-latency long-tail example established earlier in the
chapter.

Checked and reverted: drafted a correction to the SHAttered "100 GPU-years" figure using
shattered.io's "110 GPU-years" wording, then found this would create a discrepancy with the CWI
primary announcement page cited and verified earlier in this chapter's plagiarism report (which
states 100 GPU-years). Reverted to the original wording since it matches the cited primary
source; noted the source discrepancy in the plagiarism report addendum instead of picking one
without a note.

Sections reviewed and judged complete, no change made: Bayes' theorem (worked example and
base-rate-fallacy consequence both present), conditional probability, all eight named
distributions apart from the canary gap above, mean and variance, the birthday problem and hash
collision, and sharding. These carry a plain-language definition, a stated consequence, and
either a worked number or a figure tying the concept to the checkout-API narrative.

## Addendum: 2026-09-01, plain-language intuition layer

Added fourteen short plain-language analogy paragraphs (40-60 words each), one before the
notation in every major section, so a reader without calculus or summation-notation background
can follow the idea even if not every formula. Kept to the author's explicit length constraint:
3-4 lines, roughly 40-60 words. Zero em dashes, zero banned precision words; the write-time hook
caught and forced a rephrase of a stray instance during drafting. Re-ran `/plagiarism-check`;
all fourteen new paragraphs classified CLEAR. No existing prose, formulas, figures, or headings
were removed or altered.

## Addendum: 2026-09-01, sentence-level clarity pass

Fourth pass: a close, sentence-by-sentence clarity edit of the existing prose, not a content
add. Roughly 23 sentences touched across 12 locations, the heaviest of the three chapters
because several sections (Central Limit Theorem, the SHA-1/birthday paragraph, the canary-cohort
math, sharding) each carried one long, multi-clause sentence running 50-75 words. Two edits
fixed an ambiguity problem rather than just length: the sharding section's sentence buried "that
the hash function distributes keys uniformly" as a mid-sentence appositive inside a longer
clause, forcing a backtrack to parse; restructured so the assumption is stated as its own short
sentence. The SHA-1 paragraph listed "random identifiers, trace IDs, session tokens, cache keys"
as a flat comma list with no signal that the last three are examples of the first, fixed by
adding "such as." The Central Limit Theorem paragraph's three back-to-back long sentences were
each split into two or three shorter ones. No word-count reduction was a goal. Re-ran
`/plagiarism-check`; the report's addendum confirms no new matches for the rewritten phrasing.
Zero em dashes, zero banned precision words introduced. No formulas, citations, figures, or
headings touched.

## Addendum: 2026-09-01, second clarity pass

Fifth pass, a higher-bar re-read on top of the sentence-level pass above. Found and fixed one
analogy/technical mismatch and one confusing transition, both in "Selecting without replacement:
canary cohorts." The section opens with a marble-and-bag analogy, but the technical paragraph
right after it switched to "Card-probability problems" and "a specific card while drawing
several from a deck," terminology the analogy never set up. This is a leftover seam from the
pre-rewrite source, which the chapter's own earlier notes above describe as built around generic
dice, card, and coin examples that the rewrite mostly replaced; one card reference survived in
this paragraph. Reworded the paragraph's card references to marbles and a bag so the analogy and
the technical explanation stay in one frame. Second, later in the same paragraph, "So a canary
assembled by a documented random draw still carries roughly a 7% chance..." was followed by
"This is why choosing a canary cohort by hand...is a quiet source of selection bias," a causal
link that does not hold: the 7% figure is ordinary sampling variance in a correctly randomized
draw, not the reason hand-picking is biased. Replaced "This is why" with "By contrast" so the
sentence states the relationship as it stands, a contrast between two different failure modes,
not one causing the other. No other jargon-before-analogy mismatches or topic-sentence
mismatches found elsewhere in the chapter. Re-ran `/plagiarism-check`; both fixes classified
CLEAR, verdict unchanged (previous audits on this chapter remain valid). Zero em dashes, zero
banned precision words. No facts, numbers, formulas, citations, figures, or headings touched.

## Addendum: 2026-09-01, sixth pass, teaching-clarity edit

A full word-by-word re-read for teaching clarity, on top of the five prior passes. Four gaps
found and fixed, all additions grounding an existing claim rather than new content:

1. Bayes' theorem worked example (base rate 1%, sensitivity 95%, specificity 90%) stated the
   answer, roughly 8.8%, without showing the arithmetic. Added the plug-in computation
   ($P(\text{alert}) = (0.95)(0.01) + (0.10)(0.99) = 0.1085$, then $0.0095 / 0.1085 \approx
   0.088$), with a clause tying the 0.10 false-alarm rate back to the defined 90% specificity,
   so a reader can reproduce the number instead of taking it on faith.
2. The base-rate-fallacy sentence used "a detector's accuracy" where the chapter had only ever
   defined sensitivity and specificity, a term collision risk for an audience that knows
   accuracy as a distinct, precisely defined classification metric. Replaced with "sensitivity
   and specificity" to match the terms defined earlier in the same section.
3. The binomial distribution formula used $\binom{n}{x}$ with no verbal explanation of what the
   notation counts. Added a sentence reading it as "n choose x" and explaining it as a count of
   distinct successful trial orderings, applying the same in-other-words pattern used for every
   other formula in the chapter.
4. The mean and variance formulas ($E(X) = \sum x\,P(X=x)$ and $\text{Var}(X) = E(X^2) -
   (E(X))^2$) had a plain-language analogy before them but no "in other words" restatement after,
   unlike every other formula in the chapter. Added one, including a note that the variance
   formula is an algebraically equivalent shortcut for averaging squared deviations from the
   mean, so the restatement does not imply a different, wrong definition of variance.

Anti-sycophancy self-check: reviewed all four edits for dropped caveats or oversimplification
that would change technical meaning. None found; all four additions layer detail onto existing
correct claims without removing a qualifier or asserting anything the source did not support.
Zero em dashes, zero banned precision words (one draft use of a banned word caught by the
write-time hook and rephrased before saving). Re-ran `/plagiarism-check` on the four new passages;
see this chapter's plagiarism report addendum for the same date. No other sections touched.

## Addendum: 2026-09-01, image/caption/paragraph/callout formatting pass

Five changes applied throughout the chapter:
1. All seven figures moved to appear before the prose that discusses them.
2. All seven captions rewritten to state the takeaway rather than only describing the axes.
3. Visual inspection caught a real prose/default-image mismatch: the Bayes base-rate figure's
   default (frame 0) slider position was 30%, a number never mentioned anywhere in the text,
   while the prose works through a specific 1% base-rate example yielding roughly 8.8%. Fixed
   `chapter-03-probability-and-distributions-plots.py` so the figure's default frame lands on
   the 1% base rate the chapter actually computes by hand, while the slider still runs the
   full 30%-to-0.5% range for readers who want to see the collapse for themselves. All seven
   PNGs regenerated with a more robust capture method (forcing the correct default slider
   frame, waiting for two animation-frame paints before screenshotting) after finding the same
   default-frame and paint-timing issues affecting figures book-wide, not specific to this
   chapter.
4. Every paragraph split to a 3-4 line maximum.
5. Seven `.callout-*` boxes added (tip/note/important/warning), each 1-2 sentences,
   highlighting a specific gotcha, rule of thumb, or caveat already present in the prose.

No new external claims, citations, or statistics were introduced beyond the existing verified
Charig/Squire/CWI-Google sources cited elsewhere in this book. No `/plagiarism-check` web sweep
was re-run since nothing new needs verification, this addendum documents that reasoning
directly. Verdict unchanged: PUBLICATION-READY (local review only).

## Addendum: 2026-09-01, eighth pass, targeted caption and callout follow-up

Re-audited all 7 figures against their PNGs and against the placement/caption criteria from the
prior addendum. Figure placement and paragraph length were compliant chapter-wide going in
(every fig div sits before its `@fig-id` discussion paragraph, and no prose paragraph outside a
callout runs past 4 source lines), so neither needed a change this pass.

Two changes made:

1. **Caption tightened.** The normal-distribution figure's caption stated the 68-95-99.7 rule
   generalizes across scale but never named what the shaded band or slider in the image show.
   Rewritten to describe the plotted band (one standard deviation on either side of the mean,
   holding roughly 68% of the area) and what dragging the slider does, matching the same
   "what am I looking at" standard the other six captions meet.
2. **Three callouts added**, one each in the Normal, Binomial, and Poisson subsections, the only
   three named distributions without a callout of their own: a three-sigma outlier-threshold
   rule of thumb (Normal), a normal-approximation threshold for $np$ and $n(1-p)$ (Binomial), and
   a mean-equals-variance overdispersion check (Poisson). None restates an existing callout's
   point; each stays within the one-new-callout-per-subsection limit.

Anti-sycophancy self-check: verified the three-sigma figure (under 0.3%) against the chapter's
own stated 99.7%-within-three-sigma claim (100% minus 99.7% equals 0.3%, consistent). Verified
the Poisson variance-equals-mean claim against the distribution's known property and against the
chapter's own PMF formula, which depends on a single parameter $\lambda$ serving as both. Neither
addition drops a caveat or claims something the chart or surrounding text does not support. Zero
em dashes, zero banned precision words. No `/plagiarism-check` web sweep needed: the two
distribution-property callouts state standard, unattributed statistical facts implied by
material earlier in this chapter, not new external claims requiring a source.

## Addendum: 2026-09-05, gedeck-enhancement depth pass (condensed humanize scope)

Run type: condensed inline pass, scoped to the six new content blocks added in this pass only
(Lognormal distribution subsection, Chebyshev's inequality section, Joint distributions and
independence section, The Law of Large Numbers section, the new fig-clt-three-panel block
inside The Central Limit Theorem section, and the hypergeometric-distribution formalization
inside Selecting without replacement: canary cohorts), matching this chapter's own established
condensed-pass convention rather than a full multi-agent 4-pass run, since the new material is
roughly 1,700-2,000 words spread across six short sections and book mode forbids contractions
(Pattern 30 does not apply here).

Checked against the standing pattern list: em dashes (zero, confirmed by grep), AI vocabulary
list (none introduced, project write-time hook enforces this at Edit time), demonstrative
labeling and staccato stacks (none found), paragraph-terminal summaries and vague "This" subject
sentences (none found), conjunctive openers (none found), curly quotes (none found), heading
case (all new headings are sentence case, matching the existing "The Central Limit Theorem"
convention for named theorems).

Three Pattern 9 (negative parallelism, Form D exclusionary "rather than"/"instead of") instances
were found and fixed in the Joint distributions section: "assigns a probability... rather than
describing each variable on its own" rewritten to state directly what a joint distribution
captures; "makes that comparison explicit instead of leaving it inside a formula" rewritten to
"spells out that comparison directly, cell by cell"; and a definitional-negation construction in
the Law of Large Numbers section ("is not in the arithmetic... but in the guarantee") rewritten
to state the theorem's substance directly. One remaining "rather than a coincidence of two
unrelated rates" construction was kept: it draws a substantive methodological contrast (a causal
signal versus a spurious one), which is the standing exception this chapter's own conventions
carve out for "X, not Y" and "rather than" constructions.

Verdict unchanged: PUBLICATION-READY (local review only, not for public release per the standing
"do not publish" instruction on this project).
