# Humanize log: chapter-bayes-boosting.md

Mode: BOOK (auto-inferred from `books-all/` path)
Run type: condensed pass (zero-tolerance + voice), matching the pattern established for this
book's other chapters. This is a first-draft chapter awaiting author review.

## What was checked

- Em dashes: zero (structurally guaranteed by the project's write-time hook).
- The skill's standard AI-vocabulary list (see `humanize-ai-text` Pattern 7): zero hits.
- The project's own banned-precision-words list (the same fixed list of over-asserting
  adjectives and adverbs enforced across every chapter in this book by a write-time hook):
  several instances were caught and forced to a fix during drafting, per the hook's own error
  output at write time.
- Staccato stacks, demonstrative labeling, gerund headings, paragraph-terminal summaries,
  rule-of-three padding, vague "This" subjects: none found on review.
- Negative-parallelism density ("X, not Y" / "not a production-ready X"): 2 clear rhetorical
  instances found and rewritten to direct positive phrasing. A small number of remaining
  negation constructions (e.g. "BART is not gradient boosting", "fit by backfitting, not by
  gradient descent") were left as-is: each carries a technical distinction the reader needs,
  not a decorative contrast tic, matching the precedent set in Chapter 1's own QA pass.
- Contractions: none added, consistent with this book's formal ISLR-derived textbook register
  used throughout every other chapter.

## Fixes applied

1. "XGBoost still returns a single fitted ensemble, not a distribution over ensembles, because
   nothing in its fitting procedure ever samples" -> restated positively: "nothing in XGBoost's
   fitting procedure ever samples, so the output is a single fitted ensemble, a point estimate
   rather than a distribution over ensembles."
2. "Treat it as an active research direction, not a production-ready alternative to XGBoost or
   LightGBM today" -> "Treat it as an active research direction rather than something ready to
   replace XGBoost or LightGBM in production today."

## Verdict

Clean. No outstanding AI-pattern issues. Ready for author review alongside the plagiarism
report.

## Addendum: visual QA pass

Reviewed the `.py` code for all 3 figures against the prose and confirmed the math and slider
ranges are sound. Rendered each figure in a browser and found a bug in
`fig_bo_search_trajectory`: the background `go.Contour` layer, when placed inside animated
`go.Frame` objects alongside the trial-point `go.Scatter`, intermittently failed to paint on
frame transitions (reproducible: hoverable and present in the data model, but visually blank).
Fixed by making the contour a static trace outside the animation and only updating the scatter
trace on each frame, a documented Plotly.js reliability pattern for mixing Contour with
frame-based animation. Also reduced the contour grid from 80x80 to 45x45 points, a rendering-cost
improvement independent of the bug. Regenerated and reverified the fix. No new prose added, so no
plagiarism re-check needed.

## Addendum, 2026-09-05: NGBoost multimodality callout (tiny-document inline pass)

A follow-up gap-analysis pass, cross-referencing this chapter against BAP's mixture-model
chapter, added one callout note to the "NGBoost" section (see this chapter's
`.plagiarism-report.md` addendum for the content summary and source verification). At under 150
words, this addition falls under the skill's tiny-document threshold (< 400 words), so the lead
applied the check inline rather than spawning workers.

Findings on first draft, self-caught before integration:
1. "has two humps, not one": Pattern 9 Form A trailing negation ("X, not Y"). Fixed to "has two
   humps."
2. "a duration close to none of those deployments report, since the average of two clusters is
   not itself a plausible outcome": awkward phrasing plus a second Pattern 9 negation ("is not
   itself"). Fixed to "a duration that sits in the low-density gap between the two clusters, and
   one hardly any deployment reports," grounding the claim in the same bimodal-mean-in-a-gap
   framing confirmed via web search for the plagiarism pass.
3. "sidesteps this problem by never committing to a distribution shape": restructured into two
   sentences ("sidesteps this problem. It never commits to a distribution shape...") for a colon
   pivot and shorter verdict sentence, matching this chapter's established rhythm.

No em dashes, no AI-vocabulary-list hits. Checked against the project's fixed banned-precision-
word list (the same one enforced across every chapter in this book by a write-time hook): zero
hits. No contractions added, consistent with the chapter's formal register.

Verdict: clean after the one self-correction pass above.

## Addendum, 2026-09-01: what/why/how and rigor audit pass

Two sentences were revised for claim rigor, both narrow tooling-recommendation fixes rather than
new prose sections:

1. The `scikit-optimize`/`GPyOpt`/`BoTorch` sentence named all three as current options without
   noting that two of them are no longer actively maintained (skopt's repository was archived
   February 2024; GPyOpt has seen no meaningful maintenance in recent years). Verified both
   facts live via WebSearch before editing. Reworded so a reader picking a tool today knows
   which one to treat as a live default (BoTorch) versus a reference implementation.
2. The NGBoost parenthetical describing its Bernoulli output for a binary target implied the
   model produces a calibrated "uncertainty" band the way its Normal-distribution output does
   for a continuous target. A single Bernoulli parameter carries no separate variance term, so
   the phrasing overstated parity between the two cases. Reworded to state what the Bernoulli
   case delivers (a calibrated probability via a proper scoring rule) without the false
   uncertainty-band framing.

Both edits pass the same pattern checks as the rest of this log: no em dashes, no banned
precision words, no fabricated facts (both tooling-maintenance claims were verified live before
the edit). Verdict unchanged: clean, ready for author review.

## Addendum: 2026-09-01, plain-language intuition pass

Added two short (3-4 line) plain-language paragraphs before notation-heavy sections, so a reader
without a calculus background can follow the core idea: a recipe-tuning analogy before "Bayesian
hyperparameter optimization" (searching smarter using past trials instead of guessing blind),
and a two-forecasters analogy before "NGBoost" (a point prediction versus a prediction with a
stated range). Skipped "Why boosting does not have a clean posterior": "posterior" is
established vocabulary by chapter 13, and the existing paragraph explains the boosting-specific
mechanism, not a new term. No plagiarism check re-run: both additions are original, generic
analogies (recipes, weather forecasts) carrying no statistics, quotes, or external claims, so
there is nothing to verify against a source. No em dashes or banned precision words in the new
text (verified by hand). No citations, figures, or structure touched.

## Addendum, 2026-09-01: sentence-level clarity pass

A close, sentence-by-sentence pass through the full chapter, focused on wording and structure,
not content. Roughly 20 sentences were touched:

- Long sentences carrying two or three separate claims joined by "but," "and," or a semicolon
  were split at the joint, one claim per sentence: the XGBoost-regularization paragraph, the
  NGBoost distribution-parameters sentence, the `scikit-optimize`/`BoTorch` tooling paragraph,
  and the closing "Where the field stands, honestly" paragraph each split this way.
- A "neither...nor...but" sentence describing where expected improvement peaks was rebuilt as
  three short sentences (not highest here, not highest there, highest instead somewhere else),
  since the triple-negative structure made the reader hold three clauses before reaching the
  point.
- Several ambiguous "which" and "it" references were replaced with a named noun: "which turns
  out to matter" became "that geometry-aware approach turns out to matter"; "which is closer to
  what BART's tree-to-tree posterior variation captures" became "that second question is closer
  to...".
- No fact, formula, citation, or figure reference changed. All new connective phrasing is
  generic transition language, not distinctive enough to need a new plagiarism check; see this
  chapter's `.plagiarism-report.md` addendum dated the same day for the corresponding note.

Verdict unchanged: clean, ready for author review.

## Addendum, 2026-09-01: second clarity pass

A second, higher-bar read of the full chapter after the first clarity pass. Most of the chapter
needed nothing further; three small issues had survived the earlier rounds:

1. The Ridge/Lasso cross-reference sentence in "Why boosting does not have a clean posterior"
   named its Chapter 9 source as "the Bayesian Lasso chapter" while naming Chapter 4 by number
   in the same sentence, the only inconsistent chapter cross-reference in this chapter (every
   other reference in the chapter uses "Chapter N"). Changed to "Chapter 9."
2. "`GPyOpt` covered similar ground earlier" used "earlier" ambiguously, readable as either "a
   paragraph earlier" or "in the past." Reworded to "served a similar role in the past."
3. A subject-verb agreement error: "Thirty trials with this search routinely lands close to the
   region..." Corrected to "land."

Re-ran `/plagiarism-check`; see this chapter's `.plagiarism-report.md` addendum dated the same
day. No new Critical, Warning, or Note findings; all three fixes are short connective or
reference-format wording, not claims or statistics. No em dashes or banned precision words in
the new text. No fact, formula, citation, or figure changed.

Verdict: clean on a second pass. This chapter needed only minor cleanup, a legitimate outcome
for a second-round audit.

## Addendum, 2026-09-01: teaching-clarity edit pass

A full read for jargon, unstated background, and formulas left unrestated, aimed at the working
data-scientist audience rather than a stats-PhD reader. Seven edits, all additive glosses or
short restatements, no content removed:

1. "Conjugate linear regression does this in closed form" gained a four-word gloss ("one formula,
   plugged in once, with no iteration and no sampling required"), and MCMC was expanded to
   "Markov chain Monte Carlo" on its first use in this chapter (it had appeared unexpanded in the
   opening paragraph and again in "Why boosting does not have a clean posterior").
2. The Bayesian-optimization surrogate formula
   ($f(\text{learning rate}, \text{max depth}) = \text{validation loss}$) had no "in other words"
   restatement; added one, plus a sentence naming why the function cannot be written down
   directly (every query requires training a model and measuring the result).
3. "A Gaussian process" appeared with no reminder of what it does, relying on a Chapter 11
   cross-reference alone; added a one-sentence recall of the GP-as-posterior-over-curves idea.
4. The NGBoost Bernoulli-output sentence named "a proper scoring rule" without saying what makes
   a scoring rule proper; added the standard definition (minimized only when the predicted
   probability matches the true one).
5. "Functional gradient descent," in "Where the field stands, honestly," was an unglossed,
   dense term dropped into the middle of a long sentence; added a clause tying it back to the
   function-space view of boosting Chapter 8 built.
6. "Gibbs-sampling-style backfitting" got a one-sentence recall of what that means for BART
   (cycling through trees, refitting each against the others' residual), pulled out into its own
   sentence rather than a mid-clause parenthetical.
7. Self-review caught two of these additions breaking this same log's own pattern rules before
   they shipped: the Gaussian-process gloss and the backfitting gloss were each first drafted as
   a parenthetical splitting a clause's subject from its verb, the same construction earlier
   addenda in this log flagged and fixed in this chapter's opening hook equivalent. Both were
   rewritten as trailing "recall from Chapter N" sentences instead, matching the fix pattern
   established there.

Anti-sycophancy check on edit 5 specifically: the first draft claimed functional gradient descent
"reinterprets each boosting step as a step toward a distribution over predictions," a claim not
supported by the source material and not something this chapter had established. Cut before
publication and replaced with the narrower, correctly grounded claim that functional gradient
descent is gradient descent in function space, the same lens Chapter 8 used to describe ordinary
gradient boosting.

No em dashes, no banned precision words, no fabricated facts. All seven edits verified word-by-
word against a fresh grep pass. Verdict unchanged: clean, ready for author review.

## Addendum, 2026-09-01: figure-placement, caption, paragraph-length, and callout pass

Five-part formatting pass applied to all three of this chapter's figures and to every paragraph:

1. **Figure placement.** All three figure divs (`fig-bo-search-trajectory`,
   `fig-expected-improvement`, `fig-ngboost-width`) were moved to sit right after the last
   prerequisite concept paragraph in their section, ahead of the sentence that names them by
   `@fig-slug`. That naming sentence was relocated into the interpretive prose immediately
   following each figure, so a reader sees the chart before being told what to look for in it.
2. **Captions.** All three captions were rewritten to state the chart's takeaway rather than
   only its axes: the search-trajectory caption now states the spread-then-cluster pattern; the
   expected-improvement caption states where EI peaks and why; the NGBoost caption states that
   the point-estimate model's confidence looks uniform while NGBoost's interval widens past the
   dense training-data region.
3. **Visual inspection.** Opened all three PNGs (`chapter-bayes-boosting-fig-search-trajectory`,
   `-fig-expected-improvement`, `-fig-ngboost-width`) with the Read tool and checked every number,
   axis label, and direction of effect named in the surrounding prose against what each image
   shows. No mismatch found: the code in `chapter-13-bayesian-boosting-plots.py` still matches
   what the prose describes for all three figures.
4. **Paragraph length.** Every paragraph in the chapter was checked against a 50-70 word, 3-4
   line ceiling and split at natural sentence breaks where it ran long, including the "Why
   boosting does not have a clean posterior" opening, the Bayesian-optimization surrogate-function
   paragraph, the Optuna/skopt/BoTorch tooling paragraph (split once more in this pass, after the
   skopt sentence), the NGBoost mechanism paragraph, and the closing "Where the field stands"
   section. A handful of connective phrases were reworded to keep each half grammatical after a
   split (for instance "which is the same underlying view" became a new sentence, "That is the
   same underlying view").
5. **Callouts.** Three short callouts were added: a `.callout-note` after the XGBoost-
   regularization paragraph on why a penalty term is not a posterior, a `.callout-note` in the
   tooling paragraph on `scikit-optimize` and `GPyOpt`'s maintenance status, and a
   `.callout-important` after the NGBoost interpretation on why its predictive interval is not a
   substitute for BART's posterior. All three restate facts stated and audited earlier in the
   chapter's own prose; no new fact, statistic, or citation was introduced.

No em dashes, no banned precision words. No fact, formula, citation, or figure changed, only
position, caption wording, and paragraph breaks. See this chapter's `.plagiarism-report.md`
addendum dated the same day for the corresponding attribution check. Verdict unchanged: clean,
ready for author review.

## Addendum: 2026-09-01, image/caption/paragraph/callout formatting pass

Restructured all 3 figures so the image appears before the prose that discusses it (previously
prose introduced the figure, then the image, then interpretation followed; now the image comes
right after its section's setup, and both the introducing and interpreting prose follow it).
Rewrote all 3 captions to state a takeaway, not just describe axes. Split every paragraph in
the chapter to roughly 3-4 lines. Added 3 callouts (2 note, 1 important), each 1-2 sentences.

Visual inspection found and fixed two real bugs in `chapter-13-bayesian-boosting-plots.py`:
1. `fig_ngboost_predictive_width`: the dashed red "point-estimate boosted model" line was
   drawn before an identical solid blue "NGBoost predicted mean" line at every frame (both
   trace on the same y-values), so the blue line always fully covered the red one. Reordered
   the trace list so the dashed line draws on top. Also changed the default slider position
   from index 0 (interval width 0.0, meaning zero band width on first load) to index 2 (width
   1.0), since a reader who never touches the slider, including anyone viewing only the static
   PNG fallback, would otherwise see no interval at all.
2. Traced what first looked like a broken contour (a completely blank background where
   `fig_bo_search_trajectory`'s validation-loss heatmap should render) to a screenshot-timing
   issue, not a data or code bug: this figure's page auto-plays through its animation frames on
   load, and a screenshot taken at the original 600ms wait captures a mid-transition, visually
   garbled state. A much longer wait (8s, enough for the autoplay to finish and settle on its
   last frame) produces a correct, clean render every time. Re-screenshotted all 3 of this
   chapter's figures with the longer wait.

This same autoplay-on-load behavior likely affects other figures across the book beyond this
chapter's scope; flagged in the final report for the coordinator to check book-wide, since a
6-800ms screenshot wait (used for the book's original 78-figure batch) would catch the same
mid-transition artifact on any figure with enough frames for its animation to still be running
at that point.

Cast the contour's `x`/`y`/`z` arrays to plain Python lists in `fig_bo_search_trajectory`
before passing them to `go.Contour` (`plotly.py` 6.x otherwise serializes numpy arrays into a
compact binary `{dtype, bdata}` payload). This did not turn out to be the root cause of the
blank-contour symptom (the real cause was the autoplay timing above), but it is a harmless,
independently more robust way to pass the data, so the change was kept.

No new external claims were introduced by this pass (reordering, rewording, and callouts drawn
from content established earlier in the chapter), so no new plagiarism-check web verification
was run; the existing report's PUBLICATION-READY verdict stands.

## Addendum, 2026-09-01: placement/caption/paragraph/callout verification pass

Re-checked figure placement, caption quality, paragraph length, and callout coverage against
the current file state rather than assuming the prior addenda's edits were still intact.
Findings:

- **Placement**: all three `@fig-*` citation sentences sit after their div, and no paragraph
  before a div discusses that figure's specific pattern in depth ahead of it. No moves needed.
- **Captions**: opened all three PNGs directly. The search-trajectory and expected-improvement
  captions name their axes and state the chart's takeaway; left untouched. The NGBoost caption
  ("A point-estimate model reports the same confidence everywhere...") named the comparison but
  not the axes or the dense-region boundary visible in the image. Rewrote it to name both lines
  by color, the x-axis (payload size), and the roughly 5-25 KB dense-data region where the
  interval stays narrow.
- **Paragraph length**: one paragraph exceeded the ~4-line ceiling, the surrogate-function
  paragraph in "Bayesian hyperparameter optimization" (5 lines, ending in the
  $f(\text{learning rate}, \text{max depth})$ formula). Split at the sentence boundary after
  "(interpolating a noisy curve)." No wording changed, only the break.
- **Callouts**: "Where the field stands, honestly" had no callout while every other section had
  one. Added a `.callout-tip` restating the chapter's own closing recommendation (BART for a
  full posterior, Bayesian hyperparameter optimization and NGBoost for narrower problems),
  stated earlier in the surrounding prose. No other section received a new callout, to avoid
  exceeding one new callout per section and to avoid duplicating existing callout content.

The first draft of the NGBoost caption used a banned-precision-words-list adverb to describe
how closely the two lines track each other; caught by the project's write-time hook and
reworded to "closely." No em dashes in any new text. No fact, formula, citation, or claim
beyond what the chapter's own prose supports was introduced. Verdict unchanged: clean, ready
for author review.

## Addendum, 2026-09-05: quantile regression and conformal prediction sections

Mode: BOOK (auto-inferred from the `books-all/` path). Run type: scoped condensed pass over the
two new sections added this session ("Quantile regression: predicting the tail of the
distribution" and "Conformal prediction: a coverage guarantee without a posterior") plus the two
short bridging paragraphs added at the end of the NGBoost section and before "Where the field
stands, honestly." The rest of the chapter was not re-read for this pass; it carries the verdict
from every addendum above.

**Zero-tolerance patterns checked directly against the new text:**
- Em dashes: zero (write-time hook plus a manual grep of the full file after every edit).
- The project's fixed banned-precision-words list (the same over-asserting adjectives and
  adverbs enforced across every chapter in this book by a write-time hook), plus the skill's
  standard AI-vocabulary list: zero hits, verified by grep after the pass, not only at draft
  time.
- Contractions: none added, matching this book's formal ISLR-derived register (unchanged from
  every prior addendum's finding).

**Fixes applied (negative parallelism, Pattern 9, all forms):** The first draft of both new
sections leaned on trailing "X, not Y" and "rather than Y" contrast constructions more heavily
than the rest of the chapter. Nine instances were rewritten to state the positive claim directly:
1. Section heading "predicting the tail, not the average" -> "predicting the tail of the
   distribution."
2. "trained directly against that target rather than against a parametric family" -> dropped the
   trailing clause; the contrast was established in the prior sentence.
3. "That gap is not a modeling mistake" -> "That gap comes from the shape of a right-skewed
   distribution," restated as what the gap is.
4. The SLA callout's "a statement about a quantile, not a mean" -> restated both halves
   positively as two separate sentences.
5. "shows up in a monitoring dashboard, rather than in a forecast made before the load arrived"
   and "a planning question rather than an incident" -> both rewritten as direct statements of
   sequence ("well after the load ... has arrived," "before it turns into an incident").
6. "up toward the tail instead of settling at the middle of the data" -> "up toward the tail of
   the distribution," dropping the exclusionary half.
7. "for a 90% prediction interval instead of a single P95 number" -> restated as a direct
   follow-up question, no exclusion clause.
8. "Conformal prediction does not require the underlying model to be well specified" -> restated
   as what the guarantee rests on (exchangeability), with the model-misspecification point moved
   to its own positive sentence.
9. A single sentence carrying two "rather than" clauses back to back (the finite-sample
   calibration-fraction sentence) was split into two sentences, each stating what its subject
   is or does.

One "rather than" was kept on purpose: "fits a model to a chosen quantile ... rather than to its
conditional mean" is the core definitional contrast the whole section is built on, the same
precedent set earlier in this log for "BART is not gradient boosting" and "fit by backfitting,
not by gradient descent." "the relationship does not need trees" was left as-is; it is a plain
negation of necessity, not a contrast construction, and reads no differently than ordinary
technical prose.

**Numeric-claim check:** every number quoted in both sections (216 ms, 434 ms, 452 ms, 184 ms,
90%, 92%, 74%, 100%, 89%, 0.3 ms) was read directly from a script run against this chapter's own
new simulation (`chapter-13-bayesian-boosting-plots.py`, `simulate_checkout_latency`), not
estimated, rounded from memory, or stated without a run behind it. See this chapter's
`.plagiarism-report.md` for the verification trail on the three new citations (Koenker and
Bassett 1978; Vovk, Gammerman, and Shafer 2005; Romano, Patterson, and Candès 2019).

**Not covered in this pass:** live URL/citation fetch verification (folded into the plagiarism
check instead, which ran live web searches on all three new citations), and a full-chapter
critique-fix cycle (out of scope; the rest of the chapter's content and voice were not touched by
this session's edits).

Verdict: clean after the Pattern 9 fixes above. Ready for author review alongside the updated
plagiarism report.
